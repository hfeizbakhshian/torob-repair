# راهنمای ایجنت پیاده‌ساز «ترب تعمیر»

## مأموریت و مرجع

محصول، مقایسهٔ خدمات تعمیر خودرو با دستیار هوشمند است: روشن‌کردن درخواست، دریافت پیشنهاد، انتخاب متخصص، توافق و تغییر هزینه، تأیید رسیدها توسط مشتری، ارزیابی عملکرد، داوری لازم‌الاجرای AI در اختلاف حل‌نشده و بازپرداخت هزینهٔ ثبت درخواست.

`AGENTS.md` و `plan.md` بستهٔ مستقل تحویل هستند. `plan.md` مشخصات لازم برای ساخت MVP نمایشی را دارد؛ گفتگو یا `market-research.md` پیش‌نیاز اجرا نیستند. این فایل برای قواعد پایدار کار است و جزئیات عددی و قراردادها در پلن نگهداری می‌شوند.

پروژه برای چالش AI Product Engineer ترب است. مبنا متن آگهی ارائه‌شده توسط کاربر است؛ صفحهٔ https://jobs.torob.com/ai-product-engineer نیازمند حل چالش است و طبق توضیح کاربر متن آن ثابت است، پس دوباره بازش نکن. خروجی اپلای، ویدیوی حداکثر ۵ دقیقه‌ای و ۲۰۰ مگابایتی، لینک پروژه و راه تماس است. ثبت اپلای و تماس با اشخاص جزو ساخت محصول نیست.

## شروع و ادامهٔ کار

1. این فایل و کل `plan.md` را بخوان؛ وضعیت مخزن و تغییرات موجود را بررسی کن.
2. اگر مأموریت، ساخت/اجرای این پلن است، از مراحل اجرای آن شروع کن و برای تصمیم‌های بسته‌شده تأیید مجدد نگیر. مأموریت ویرایش مستندات به‌تنهایی مجوز نصب، ساخت یا استقرار نیست.
3. پیش‌فرض‌های اجرایی پلن را مبنا بگیر. اعداد اولیه سیاست قابل تنظیم‌اند، نه یافتهٔ تأییدشدهٔ بازار. کیفیت واقعی را از نتیجهٔ آزمون جداگانه گزارش کن.
4. جزئیات کم‌خطر کدنویسی، نام‌گذاری و اجزای رابط را با قضاوت خودت حل کن. فقط تعارضی که اجرای خواستهٔ کاربر را واقعاً ناممکن می‌کند مطرح شود؛ بخش‌های مستقل را ادامه بده.
5. دستور جدید کاربر بر مستندات مقدم است. کار نامرتبط دیگران را بازنویسی نکن. ابزار ساخت پروژه نباید این راهنما یا پلن را با فایل پیش‌فرض جایگزین کند.

## دامنهٔ مصوب برای ساخت

- استک قطعی: Next.js/React/TypeScript و Tailwind برای رابط؛ Python 3.13، FastAPI و Pydantic برای بک‌اند؛ PostgreSQL 18 با SQLAlchemy 2، asyncpg و Alembic برای داده. Node.js 24 و npm برای رابط و uv برای پایتون استفاده شوند.
- انتخاب بک‌اند پایتونی با ترجیح صریح کاربر و راحتی نگهداری و توسعهٔ اتصال AI در Python هماهنگ است. دامنه، مجوز، نشست، محاسبه، پرداخت و اتصال AI فقط در بک‌اند پایتونی باشند؛ Next.js مسئول رابط و عبور درخواست‌های `/api` است.
- اتصال مدل با استفادهٔ محدود از LangChain، بسته‌های langchain-core و langchain-deepseek، پشت قرارداد مستقل AiProvider انجام شود. AiService پایتونی مالک مراحل، بودجه، timeout و retry است؛ فراخوانی async مدل و خروجی ساختاریافته طبق پلن انجام شوند. retry پنهان و حلقهٔ خودمختار فعال نشوند؛ CrewAI و ساخت جریان LangGraph جزو MVP نیستند.
- worker پایتونی از همان سرویس‌های دامنه استفاده کند و کارهای پایدار در جدول Job در PostgreSQL بمانند. برای MVP صف مستقل، Redis یا Celery اضافه نکن. PostgreSQL محلی با Docker Compose یا نصب موجود و DATABASE_URL اجرا شود.
- قرارداد Pydantic/OpenAPI مرجع دادهٔ API و منبع تولید typeهای رابط است. آزمون بک‌اند با pytest روی PostgreSQL و آزمون مسیرهای کاربر با Playwright اجرا شود؛ جزئیات راه‌اندازی و کنترل قرارداد در پلن آمده‌اند.
- دمو برای تهران، ۲۰۶ تیپ ۵ و سه قالب کلاچ، ترمز جلو و عیب‌یابی خنک‌کاری در تعمیرگاه ساخته شود. این پوشش نمونه است؛ حوزهٔ محصول تعمیرات خودرو باقی می‌ماند.
- پرداخت و هویت نمونه‌اند، ولی نشست، مجوز، دادهٔ پایدار و قواعد سرور باید واقعی و قابل آزمون باشند. عدد تعرفهٔ دمو، قیمت واقعی خدمت معرفی نشود.
- اجرای بدون کلید با AI نمایشی الزامی است؛ آداپتر واقعی DeepSeek Flash طبق پلن پیاده شود. نبود کلید مانع تحویل mock نیست و پاسخ ساختگی نباید اجرای واقعی مدل معرفی شود.
- اتصال قطعات در MVP، لینک ترب و اطلاعات ثبت‌شدهٔ دستی است. خزش، API عمومی فرضی، خرید خودکار و ادعای موجودی یا فروشندهٔ نزدیک بدون داده نساز.
- اشتراک خصوصی متخصص، RAG و زیرساخت برداری، OCR تصویر، صوت، درگاه بانکی، پیامک، انتشار عمومی و اپ بومی خارج از MVP هستند. ایدهٔ مشورت خصوصی در توسعهٔ بعدی باید بودجه‌ای مستقل از مشتری داشته باشد و امتیاز خریدنی ایجاد نکند.

## قواعد غیرقابل نقض محصول

- همهٔ متخصصان مرتبط درخواست را می‌بینند و پیشنهاد ساختاریافته می‌دهند؛ فقط منتخب پس از پذیرش دوطرفه چت اجرایی دارد. تعداد پیشنهادها، سهمیهٔ AI مشتری را ضرب نمی‌کند.
- قیمت پیشنهاد آزاد است؛ بازه، کف و تأیید AI شرط ثبت یا انتخاب نیستند. آستانهٔ آماری انصاف در پلن فقط برای بررسی بازپرداخت است.
- تغییر توافق قبل از مراجعه و در محل ممکن است. توافق عادی با تأیید صریح هر دو طرف فعال می‌شود؛ سکوت یا تأیید نسخهٔ قدیمی کافی نیست. AI توافق دوطرفه را وتو نمی‌کند. اگر اختلاف با توافق حل نشد، AI حکم می‌دهد و حکم معتبر طبق پلن بدون تأیید مجدد طرفین اعمال می‌شود؛ نتیجهٔ داوری با تأیید مشتری یا توافق داوطلبانه یکسان ثبت نشود.
- افزایش بی‌دلیل قیمت باعث افت امتیاز می‌شود؛ زنجیرهٔ ارزیابی از نسخهٔ پیشنهاد منتخب تا اولین توافق و تغییرات بعدی است. اختلاف فاحشِ مستند قیمت فاکتور قطعه با قیمت هم‌مبنای ترب نیز به مشتری اطلاع داده و در امتیاز ثبت شود. کاهش قیمت، قیمت اولیهٔ بالا یا بازپرداخت به‌تنهایی اثر منفی ندارند. جریمهٔ مالی و تعلیق مبتنی بر این امتیاز نساز.
- رسید و ریز اقلام باید به تأیید صریح مشتری روی همان نسخه برسند؛ تأیید اصالت توسط AI لازم نیست. رد رسید به مسیر اختلاف می‌رود؛ حکم AI دربارهٔ هزینه، تأیید مشتری جعل نمی‌کند. قیمت ترب برای مقایسه باید منشأ، زمان و مشخصات هم‌ارز داشته باشد؛ دادهٔ ناقص یا تأییدنشده، امتیاز منفی خودکار نسازد.
- زمان مراجعه هنگام پیشنهاد، انتخاب، پذیرش و شروع دوباره کنترل شود. نوبت گذشته قابل انتخاب نیست و جابه‌جایی زمانِ همکاری پذیرفته‌شده تأیید دو طرف می‌خواهد؛ مهلت دریافت پیشنهاد بازنشانی نشود.
- قیمت قطعه و زمان کار مبنای برآوردند. مبلغ کل خدمت و مبلغ قابل پرداخت به متخصص جدا هستند؛ قطعهٔ خریده‌شده توسط مشتری دوباره به حساب متخصص اضافه نشود.
- خوداظهاری، رسید استخراج‌شده و دادهٔ معتبر یکسان نیستند. مرجع بازار از تعمیرهای مشابه انجام‌شده با کیفیت مشخص ساخته می‌شود. نمونهٔ نمایشی وارد مرجع واقعی نشود.
- مهلت دریافت پیشنهاد حداقل ۲۴ ساعت است. نبود پیشنهاد معتبر مسیر بازپرداخت دارد. در حالت bootstrap، درخواست مشتری بدون بررسی قیمتی AI پذیرفته می‌شود؛ در سیاست مبتنی بر داده، وجود یک پیشنهاد معتبر و منصفانه مانع بازپرداخت قیمتی است.
- بررسی بازپرداخت رایگان و مستقل از سهمیهٔ چت است. استحقاق با انتقال موفق وجه فرق دارد و اجرت و قطعه خودکار مشمول نیستند. مهلت و مسیر رسیدگی نامعلوم را دقیقاً طبق پلن اجرا کن.
- مشتری در مدل کسب‌وکار میانگین هزینهٔ API را می‌پردازد. سه مرحله هرکدام حداکثر ۳ نوبت دارند؛ سقف‌ها باید سمت سرور رزرو و ثبت شوند. تخمین ۲۰ هزار ورودی/۵ هزار خروجی اندازه‌گیری واقعی نیست. جدول تعرفهٔ مدل‌ها در پلن اضافه نشود.
- پرسش‌های اولیهٔ لازم یکجا ارائه شوند. کمبود اطلاعات به بررسی حضوری یا فرم دستی منتهی شود، نه تشخیص قطعی یا قیمت‌سازی.

## کیفیت اجرا و تحویل

- منطق مالی، نسخه، زمان و مجوز را در سرویس دامنه و تراکنش پیاده کن؛ به رابط یا نظر مدل برای اجرای آن‌ها تکیه نکن.
- خروجی AI و تمام ورودی‌های کاربر اعتبارسنجی شوند. متن رسید و متخصص دستور سیستم نیست. مدل مستقیماً وجه منتقل یا مجوز تغییر نمی‌دهد؛ حکم داوری معتبر را سرویس دامنه به‌صورت نسخه‌دار و تکرارایمن اعمال می‌کند. در MVP اعمال حکم یعنی ثبت و ابلاغ نتیجه و مبلغ تسویه؛ وصول واقعی وجه تعمیر ساخته نشود.
- در آداپتر LangChain، مصرف واقعی حتی برای پاسخ نامعتبر ثبت شود؛ تعداد تلاش‌ها، پارامترهای مدل و نبود retry داخلی با شبیه‌سازی ارتباط شبکه آزموده شوند. ثبت مصرف و نتیجه در PostgreSQL باشد و tracing بیرونی به‌صورت پیش‌فرض غیرفعال بماند.
- مسیر موفق، خطا، هم‌زمانی، تکرار عملیات و قطع سرویس را طبق معیارهای پلن آزمون کن. mock برای آزمون پیش‌فرض و ساعت تزریقی برای مهلت‌ها استفاده شود؛ تست پیش‌فرض مصرف پولی ندارد.
- داده، قیمت، ارزیابی و پرداخت نمونه در محصول برچسب داشته باشند. وابستگی رسمی به ترب، بی‌رقیب‌بودن ایده یا نبود خدمات خودرو در ترب ادعا نشود؛ نقد آچاره تحقیق مستقل تأییدشده نیست.
- کلید، نشست و اطلاعات خصوصی را در مخزن یا log ثبت نکن. مجوز مدارک و لغو دسترسی متخصص قبلی را سمت سرور اعمال کن.
- نصب سرویس بیرونی، خرید اعتبار، ارسال پیام به اشخاص، اپلای یا انتشار عمومی را بدون دستور مربوط انجام نده. نبود این‌ها مانع دموی محلی نیست.
- تغییرات را تا اجرای مسیر کامل، گذراندن بررسی‌های لازم و نوشتن README و راهنمای دمو ادامه بده. صفحهٔ ظاهری یا دکمهٔ بی‌عمل تحویل کامل محسوب نمی‌شود.
- گفتگو، رابط و مستندات فارسی باشند؛ کد و شناسه‌ها انگلیسی بمانند. نتیجهٔ نهایی شامل راه اجرا، آزمون‌های واقعاً انجام‌شده و محدودیت مهم باشد.

## نگهداری دو سند

متن را مشخصات یکپارچهٔ محصول نگه دار؛ تاریخچهٔ گفتگو، دورهای طراحی و قواعد منسوخ اضافه نکن. تغییر تصمیم را در بخش اصلی جایگزین و بخش‌های وابسته را هماهنگ کن. جزئیات متغیر را فقط در `plan.md` نگه دار. تاریخ و وضعیت را واقعی ثبت کن؛ آماده‌بودن پلن به معنی ساخته‌شدن یا اعتبارسنجی بازار نیست.
