# راهنمای ضبط دموی پنج‌دقیقه‌ای

سناریوی ضبط برای ویدیوی حداکثر ۵ دقیقه‌ای. همهٔ اعداد و حساب‌ها نمونه‌اند و در ویدیو هم
باید همین‌طور معرفی شوند.

## آماده‌سازی پیش از ضبط

```bash
cp .env.example .env
docker compose up -d db
cd backend && uv sync --locked && uv run alembic upgrade head && uv run python -m app.seed
cd ../frontend && npm ci && npm run api:types
cd .. && uv run --project backend python scripts/dev.py --with-db
```

بررسی‌های آخر:

- <http://127.0.0.1:8000/api/health> باید `{"status":"ok","appMode":"demo","aiMode":"mock"}`
  بدهد.
- worker در حال اجرا باشد؛ بدون آن مهلت ۲۴ ساعته اعمال نمی‌شود.
- سه تب مرورگر با پروفایل‌های جدا باز کنید تا نشست‌ها قاطی نشوند: **مشتری**، **متخصص**،
  **پشتیبانی**. (در Chrome: سه پنجرهٔ Incognito جدا.)
- بزرگ‌نمایی مرورگر روی ۱۲۵٪ تا متن فارسی در ویدیو خوانا باشد.
- برای نمایش رابط موبایل، یک تب با عرض ۳۶۰ پیکسل آماده داشته باشید.

> اگر می‌خواهید از صفر شروع کنید، `docker compose down -v` داده را پاک می‌کند. این کار
> اختیاری است و در شروع عادی هیچ داده‌ای پاک نمی‌شود.

---

## خط زمانی ۵ دقیقه‌ای

### ۰:۰۰ — ۰:۲۵ مسئله (۲۵ ثانیه)

روی صفحهٔ `/` بمانید.

> «وقتی خودروی شما خراب می‌شود، معمولاً نمی‌دانید چه چیزی قرار است تعویض شود و چرا
> صورت‌حساب آخر با چیزی که شنیده بودید فرق دارد. ترب تعمیر همین را هدف گرفته: اقلام و
> شرایط قیمت را روشن می‌کند و هر تغییر هزینه را نسخه‌دار ثبت می‌کند.»

بنر بالای صفحه را نشان دهید و بگویید این نصب نمایشی است.

### ۰:۲۵ — ۱:۰۵ ثبت درخواست (۴۰ ثانیه)

تب **مشتری** — ورود با `customer-sahar` → «شروع درخواست تعمیر».

1. خدمت «بررسی و تعویض کیت کلاچ»، محله «تهرانسر»، و نشانه‌ها را بنویسید.
2. «ادامه و مشاهدهٔ هزینه» را بزنید.

> «قبل از هر فراخوانی هوش مصنوعی، مبلغ بسته، سقف نوبت هر مرحله و سیاست بازپرداخت را
> می‌بینم. مبلغ ۲٬۰۰۰ تومان و پرداخت آزمایشی است.»

3. پرداخت آزمایشی → پرسش‌ها یکجا می‌آیند. برچسب «پاسخ AI نمایشی» را نشان دهید.
4. پاسخ‌ها را انتخاب کنید → «ساخت خلاصهٔ درخواست» → «تأیید و انتشار».

> «انتشار خودش تأیید همین خلاصه است — یک دکمه، و بدون مصرف سهمیهٔ هوش مصنوعی.»

### ۱:۰۵ — ۱:۵۵ مقایسهٔ پیشنهادها (۵۰ ثانیه)

تب **متخصص** — با `specialist-arya` وارد شوید، `/specialist` را باز کنید.

> «متخصص فقط خلاصهٔ فنی، محله و بازهٔ زمانی را می‌بیند. پیشنهاد رقبا، تماس و مدارک خصوصی
> در دسترس او نیست.»

پیشنهاد را ثبت کنید. همین کار را با `specialist-behnam` (گران‌تر) و `specialist-kaveh`
(ارزان‌تر) تکرار کنید.

تب **مشتری** — به پروندهٔ خود برگردید.

> «قیمت آزاد است؛ هیچ کف، سقف یا تأیید هوش مصنوعی شرط ثبت پیشنهاد نیست.»

- سه مرتب‌سازی (ارزان‌ترین، زودترین نوبت، بالاترین امتیاز) را نشان دهید.
- روی «ریز اقلام و شرایط» یکی از پیشنهادها کلیک کنید و **«سابقهٔ کافی نیست»** را نشان
  دهید.

> «امتیاز هرگز به‌صورت عدد تنها نمایش داده نمی‌شود: مخرج و تفکیک علت‌ها همیشه کنارش است، و
> زیر پنج پروندهٔ قابل ارزیابی اصلاً عددی نشان داده نمی‌شود.»

### ۱:۵۵ — ۳:۰۰ توافق، رسید و هشدار قیمت (۶۵ ثانیه)

تب **مشتری** — تیک شرط داوری را بزنید و ارزان‌ترین را انتخاب کنید.

> «تا وقتی شرط داوری را نپذیرم، دکمهٔ انتخاب غیرفعال است.»

تب **متخصص** (همان متخصص منتخب) — «پذیرش همکاری».

> «پذیرش انتخاب به‌تنهایی مجوز تعمیر نیست؛ هنوز باید توافق به تأیید هر دو طرف برسد.»

- متخصص پیش‌نویس توافق را می‌سازد و تأیید می‌کند.
- **مشتری** تأیید می‌کند → نسخه فعال می‌شود. کارت «تأییدهای همین نسخه» را نشان دهید.

در همان صفحه، بخش «پرسش دربارهٔ این همکاری» را نشان دهید و یک پرسش بپرسید.

> «این مرحلهٔ دوم گفتگوست و سهمیه‌اش میان مشتری و متخصص منتخب مشترک است؛ نفر دوم سهمیه را
> دو برابر نمی‌کند. پاسخ فقط سوابق پرونده را توضیح می‌دهد و مبلغ توافق را تغییر نمی‌دهد.»

تب **پشتیبانی** — `/support` → «+۴۹ ساعت» تا نوبت مراجعه فرا برسد.

> «ساعت دمو فقط به جلو می‌رود و در پایگاه ذخیره می‌شود، پس مهلت‌ها بدون انتظار واقعی
> آزمون می‌شوند.»

تب **متخصص** — «ثبت شروع کار» → در صفحهٔ «مخارج، رسید و پایان» متن مخارج را بنویسید و
«استخراج اقلام» را بزنید.

> «متن به اقلام تبدیل می‌شود، ولی پیش از اینکه مشتری آن را ببیند، خود متخصص بازبینی
> می‌کند.»

تب **مشتری** — نسخهٔ رسید را **رد** کنید با دلیل.

> «تصمیم مشتری به همین نسخه تعلق دارد. اگر متخصص نسخه را ویرایش کند، تأیید قبلی منتقل
> نمی‌شود. تأیید رسید هم به‌تنهایی مجوز افزایش مبلغ توافق نیست.»

### ۳:۰۰ — ۳:۵۵ داوری و امتیاز (۵۵ ثانیه)

تب **مشتری** — `/requests/<id>/dispute` → قلم مورد اختلاف را با مبلغ و دلیل ثبت کنید.

هر دو طرف اظهارات را بنویسند و «اعلام پایان اظهارات من» را بزنند.

> «تا پایان فرصت اظهارات یا اعلام پایان هر دو طرف، داوری آغاز نمی‌شود.»

«درخواست صدور حکم» را بزنید.

> «دور اول ممکن است فهرست اطلاعات لازم را برگرداند؛ در این حالت هیچ تسویه‌ای اعمال
> نمی‌شود.»

اگر وضعیت `awaiting_ai` شد، در تب **پشتیبانی** «تمدید یک‌بارهٔ بودجهٔ داوری» را بزنید و
دوباره حکم بخواهید.

> «پشتیبان شواهد را تکمیل و مسیر را باز می‌کند، ولی جانشین صادرکنندهٔ حکم نیست و حکم تسویه
> را بازنویسی نمی‌کند.»

حکم و کارت «نتیجهٔ تسویه» را نشان دهید:

> «حکم بدون تأیید مجدد طرفین اعمال می‌شود، ولی سرویس دامنه پیش از اعمال آن را
> اعتبارسنجی می‌کند: فقط دربارهٔ اقلام مورد اختلاف، بدون قلم تازه، و بدون مبلغی بیشتر از
> ادعا. مبلغ نهایی را کد محاسبه می‌کند. توجه کنید که نظر ثبت‌شدهٔ مشتری دربارهٔ رسید عوض
> نشده، و اجرای حکم یعنی ثبت و ابلاغ نتیجه — وصول وجه تعمیر در این نسخه ساخته نشده است.»

### ۳:۵۵ — ۴:۲۵ بازپرداخت (۳۰ ثانیه)

تب **مشتری** — در یک پروندهٔ دیگر `/requests/<id>/refund`.

> «بررسی بازپرداخت هزینهٔ ثبت درخواست رایگان است و به سهمیهٔ چت وابسته نیست. در حالت
> bootstrap — یعنی وقتی مرجع قیمتی کافی نداریم — درخواست مشتری بدون بررسی قیمتی هوش
> مصنوعی پذیرفته می‌شود، حتی اگر پیشنهاد منصفانه‌ای وجود داشته باشد.»

وضعیت و برچسب «انتقال آزمایشی» را نشان دهید.

> «استحقاق و انتقال دو رخداد مستقل‌اند و هر پرداخت حداکثر یک بازپرداخت کامل دارد.»

### ۴:۲۵ — ۵:۰۰ تصمیم‌های فنی و محدودیت‌ها (۳۵ ثانیه)

تب **پشتیبانی** — بخش «سنجه‌های داخل محصول» و فهرست «سناریوهای قابل اجرا با این کنترل‌ها».

> «کنار هر نسبت، مخرج آن هست؛ اگر مخرج صفر باشد «داده‌ای نیست» نوشته می‌شود.»

جمع‌بندی:

> «قواعد مالی، نسخه، زمان و مجوز در سرویس دامنه و تراکنش پیاده شده‌اند، نه در رابط و نه
> با تکیه بر نظر مدل. خروجی مدل و همهٔ ورودی‌های کاربر اعتبارسنجی می‌شوند و متن رسید برای
> سیستم دستور نیست. اجرای پیش‌فرض بدون هیچ کلیدی کار می‌کند و پاسخ‌های نمایشی همه‌جا برچسب
> دارند. پرداخت شبیه‌سازی‌شده است، اتصال به ترب دستی است، و کیفیت حالت live هنوز آزموده
> نشده — برای همین یک مجموعهٔ ۲۴ پرونده‌ای برچسب‌خورده برای سنجش مدل واقعی آماده است.»

در پایان تب موبایل ۳۶۰ پیکسلی را نشان دهید.

---

## اگر چیزی در ضبط خراب شد

| نشانه | کار |
| --- | --- |
| مهلت‌ها اعمال نمی‌شوند | worker اجرا نیست؛ `uv run python -m app.worker` |
| «برای این اقدام باید وارد حساب نمونه شوید» | نشست آن تب تمام شده؛ دوباره وارد شوید |
| نوبت مراجعه هنوز نرسیده | در `/support` ساعت را جلو ببرید |
| می‌خواهید از صفر شروع کنید | `docker compose down -v` و سپس migration و seed |

## چیزهایی که در ویدیو **نباید** ادعا شوند

- وابستگی رسمی به ترب، یا اتصال زنده به قیمت و موجودی ترب.
- اینکه قیمت‌ها، پرداخت‌ها یا مرجع بازار واقعی‌اند.
- اینکه پاسخ نمایشی خروجی مدل واقعی است، یا اینکه کیفیت حالت live سنجیده شده است.
- اینکه این ایده بی‌رقیب است یا خدمات فنی خودرو در ترب وجود ندارد.

## یادداشت دربارهٔ ساعت دمو

«بازنشانی ساعت» تنها جایی است که ساعت دمو به عقب می‌رود. برای اینکه کارهای زمان‌بندی‌شده
در همان لحظه در آینده جا نمانند، سررسید کارهای در صف هم به همان اندازه جابه‌جا می‌شود.
اگر در میانهٔ ضبط ساعت را بازنشانی کردید، انتظار داشته باشید کارهای معوق ظرف چند ثانیه
توسط worker اجرا شوند.

## تست اتصال واقعی از مسیر AvalAI

برای تفکیک زبان ورودی، `app.check_avalai --live` ورودی انگلیسی و خروجی فارسی دارد؛
تست مستقلِ ورودی فارسی با `.venv/bin/python -m app.check_avalai_fa --live` از `backend`
اجرا می‌شود و مدل‌های مجاز کلید را با یک ورودی مقایسه می‌کند؛ علاوه بر مصرف و وضعیت، متن
پاسخ و `smokePassed` هر مدل و خلاصهٔ مقایسه نمایش داده می‌شود. مدلِ خارج از فهرست درگاه،
ناموجود گزارش می‌شود و به مدل پیش‌فرض جایگزین نمی‌شود. با `--model` می‌توان فقط یک شناسهٔ
مشخص را آزمود.

پس از تنظیم متغیرهای AvalAI طبق README، در `backend` دستور
`uv run python -m app.check_avalai --live` را اجرا کنید. این آزمون یک درخواست مصنوعی
می‌فرستد و وضعیت پایان، مصرف و انطباق schema را نشان می‌دهد. تنها در صورت موفقیت، پاسخ را
اجرای واقعی مدل معرفی کنید. برای ضبط مسیر کامل محصول با AI واقعی، تنظیمات README را اعمال
و API و worker را دوباره اجرا کنید؛ حالت سنجش محلی با `AI_ENFORCE_COST_CAP=false` سقف
هزینهٔ روزانه ندارد. خطای بالادست یا موفقیت تست سادهٔ اتصال، نتیجهٔ سنجش کیفیت ۲۴ پرونده‌ای
نیست.

در اجرای ۲۰۲۶-۰۹-۲۱ روی این نصب، `glm-5.3-flash` این تست را با پاسخ فارسیِ منطبق با
schema گذراند و `deepseek-v4.1-flash` به‌دلیل عبور خروجی از رزرو رد شد؛ جزئیات در README.
