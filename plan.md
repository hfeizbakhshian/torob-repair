# پلن اجرایی «ترب تعمیر» — تعمیرات خودرو

آخرین ویرایش: ۲۰۲۶-۰۹-۲۰  
وضعیت: آمادهٔ تحویل به ایجنت برای ساخت MVP نمایشی؛ هنوز نرم‌افزار پیاده‌سازی نشده است.

این سند و `AGENTS.md` برای شروع پیاده‌سازی کافی‌اند؛ مطالعهٔ گفتگو یا فایل تحقیق بازار پیش‌نیاز نیست. قواعد محصول و استک تعیین‌شده مبنای اجرا هستند. مقادیر عددی و دامنهٔ نمونه، تنظیمات اولیه برای رفع ابهام‌اند و اعتبارسنجی بازار محسوب نمی‌شوند؛ سیاست‌ها در تنظیمات نسخه‌دار نگهداری شوند. برای انتخاب‌های بسته‌شده دوباره سؤال نپرس.

## ۱. هدف و محدودهٔ تحویل

ترب تعمیر، درخواست تعمیر خودرو را با کمک AI روشن می‌کند، پیشنهاد متخصصان را قابل مقایسه می‌کند و توافق و تغییر هزینه را ثبت می‌کند. ارزش اصلی، فهمیدن اقلام و شرایط قیمت و کاهش غافلگیری هزینه است. AI عملکرد و اعتراض قیمتی را ارزیابی می‌کند و اگر مشتری و متخصص اختلافشان را با توافق حل نکنند، حکم لازم‌الاجرا می‌دهد. توافق داوطلبانهٔ طرفین همچنان بدون تأیید AI ممکن است؛ داوری، مسیر مستقل پایان اختلاف است.

مشتری هدف، مالک خودروی سواری با دانش فنی محدود است. متخصص با درخواست کامل‌تر، فرم پیشنهاد و ثبت سادهٔ مخارج کار می‌کند. وعدهٔ محصول، تشخیص قطعی خرابی، تضمین کیفیت تعمیر یا ثابت‌ماندن همیشگی قیمت نیست.

این پروژه برای چالش [AI Product Engineer ترب](https://jobs.torob.com/ai-product-engineer) است. متن آگهی توسط کاربر ارائه شده، ثابت است و نیازی به بازکردن صفحهٔ دارای چالش نیست. تحویل اپلای شامل ویدیوی حداکثر ۵ دقیقه‌ای و ۲۰۰ مگابایتی، لینک پروژه و راه تماس است. ارسال اپلای جزو کار پیاده‌سازی نیست.

در تحقیق بازار، دستهٔ خدمات فنی خودرو در ترب دیده شده است. ادعای فقدان کامل این بازار، بی‌رقیب‌بودن ایده یا وابستگی رسمی به ترب نکن. نقد آچاره در گفتگو، فرضیهٔ مسئله است و شاهد مستقل رقابتی نیست.

### تصمیم‌های اجرایی MVP

| موضوع | مبنای ساخت |
| --- | --- |
| خروجی | وب‌اپ کامل فارسی و راست‌به‌چپ، قابل اجرا روی رایانهٔ محلی و قابل استفاده با مرورگر موبایل |
| پوشش نمونه | تهران، خودروی پژو ۲۰۶ تیپ ۵؛ محل انجام خدمت تعمیرگاه است. تهرانسر محلهٔ نمونهٔ مشتری است |
| خدمات نمونه | بررسی و تعویض کیت کلاچ، بررسی و تعویض لنت ترمز جلو، عیب‌یابی سیستم خنک‌کاری؛ هرکدام قالب مستقل |
| پوشش خارج از نمونه | قبل از پرداخت، پیام «فعلاً در پوشش این دمو نیست»؛ فرم‌ها و جداول برای افزودن پوشش داده‌محور باشند |
| پرداخت | شبیه‌ساز سمت سرور؛ مبلغ نمونهٔ ثبت درخواست ۲٬۰۰۰ تومان با برچسب «آزمایشی، بدون انتقال وجه» |
| هویت | حساب‌های نمونهٔ مشتری، متخصص و پشتیبان؛ نشست و کنترل دسترسی واقعی سمت سرور، بدون پیامک و ثبت‌نام عمومی |
| AI | پیش‌فرض `mock` برای اجرای بدون کلید؛ اتصال اختیاری واقعی به DeepSeek Flash از طریق LangChain با کلید و تنظیمات سرور؛ کنترل مراحل و مصرف در بک‌اند |
| قطعات ترب | لینک جست‌وجوی ترب + ثبت دستی اطلاعات لینک محصول؛ فروشندگان نمونه فقط با برچسب نمونه، بدون خزش یا ادعای موجودی زنده |
| اشتراک مشورت خصوصی | خارج از MVP؛ در رابط پرداخت یا دکمهٔ غیرفعال برای آن نساز |
| زمان و هزینهٔ ساخت | برنامهٔ اجرا براساس مراحل و معیار پذیرش است؛ تاریخ یا بودجهٔ واقعی اختراع نشود. اجرای پیش‌فرض به سرویس پولی نیاز ندارد |

این انتخاب‌ها پوشش دمو را مشخص می‌کنند؛ حوزهٔ محصول تعمیرات خودرو باقی می‌ماند. توسعه به خودروها و مناطق دیگر، تعمیر در محل مشتری، اشتراک خصوصی، OCR تصویر، صوت، خرید خودکار قطعه، پرداخت واقعی، امانت‌داری وجه تعمیر، اپ بومی و RAG جزو تحویل فعلی نیستند.

## ۲. مسیر کامل و صفحات محصول

پیش از اولین فراخوانی AI، مشتری مبلغ بسته، حداکثر سه نوبت در هر مرحله و سیاست بازپرداخت را می‌بیند و پرداخت آزمایشی را انجام می‌دهد. فرم مشخصات اولیه و بررسی پوشش بدون AI و رایگان‌اند. پرداخت به پیش‌نویس پرونده متصل است؛ انتشار فقط با تأیید خلاصهٔ مشتری انجام می‌شود.

1. **شرح و تکمیل:** مشتری خودرو، خدمت احتمالی، نشانه‌ها، محله و بازهٔ مراجعه را ثبت می‌کند. اولین پاسخ AI حداکثر ۸ سؤال لازم را یکجا و عمدتاً چندگزینه‌ای یا کوتاه‌پاسخ می‌پرسد. نوبت دوم خلاصه و ابهام‌ها را می‌دهد؛ نوبت سوم فقط اصلاح ضروری است. تأیید خلاصه دکمهٔ مستقل و بدون مصرف AI دارد.
2. **انتشار:** درخواست برای همهٔ متخصصان فعال و مرتبط قابل مشاهده می‌شود. ارتباط یعنی اشتراک خدمت، خودرو، شهر و نوع مراجعه؛ نزدیکی محله فقط عامل مقایسه است. داشبورد با تازه‌سازی دوره‌ای کار می‌کند و ارسال پیامک یا پیام به خارج سامانه ندارد.
3. **پیشنهاد:** هر متخصص یک پیشنهاد فعال با تاریخچهٔ نسخه‌ها ثبت می‌کند. قیمت آزاد است. فرم، نوع قیمت، اقلام، زمان، ضمانت و شرایط را می‌گیرد. پیشنهاددهی عادی چت اجرایی یا فراخوانی AI ایجاد نمی‌کند.
4. **مقایسه و انتخاب:** مشتری پیشنهادهای معتبر را می‌بیند و یکی را انتخاب می‌کند. متخصص ظرف ۲ ساعت پذیرش انتخاب را ثبت می‌کند؛ فقط پس از آن چت اجرایی او فعال می‌شود. اعتبار خود پیشنهاد و توافق اولیه جداگانه در صفحه مشخص‌اند.
5. **توافق:** متخصص نسخهٔ پیش‌نویس دامنه و هزینه را می‌سازد؛ مشتری و متخصص هر دو همان نسخه را تأیید می‌کنند. پذیرش انتخاب به‌تنهایی مجوز تعمیر نیست.
6. **مراجعه و تغییر:** پیش از مراجعه یا در تعمیرگاه، تغییر اقلام و هزینه با دلیل و مدرک ثبت می‌شود. دو تأیید صریح نسخهٔ جدید را فعال می‌کند؛ نظر AI شرط آن نیست. انصراف در هر دو مرحله ممکن است.
7. **مخارج و پایان:** متخصص مخارج و زمان واقعی را وارد می‌کند، AI متن را به اقلام تبدیل می‌کند و متخصص نتیجه را بازبینی می‌کند. مشتری رسیدها و اقلام همان نسخه را تأیید یا رد می‌کند و سپس صورت‌حساب را تأیید یا مغایرت را گزارش می‌کند. اختلاف فاحشِ مستند قیمت قطعه با ترب به مشتری اطلاع داده و در امتیاز متخصص ثبت می‌شود. بی‌پاسخی، پایان موفق یا تأیید هزینه محسوب نمی‌شود.
8. **اختلاف و داوری:** ابتدا امکان اصلاح و توافق وجود دارد؛ در صورت حل‌نشدن اختلاف، AI براساس سوابق و اظهارات طرفین حکم می‌دهد. حکم معتبر بدون تأیید مجدد اجرا و جدا از توافق داوطلبانه ثبت می‌شود.
9. **ارزیابی و بازپرداخت:** ارزیابی مستقل رفتار و مسیر رایگان بازپرداخت مطابق بخش‌های ۵ و ۶ انجام می‌شوند؛ وضعیت توافق، داوری، ارزیابی و پرداخت از هم جداست.

### نقشهٔ صفحات

| مسیر | محتوای ضروری |
| --- | --- |
| `/` | توضیح کوتاه ارزش، پوشش نمونه، شروع درخواست و ورود به حساب نمونه |
| `/requests/new` | مشخصات، پوشش، مبلغ بسته، پرداخت آزمایشی، پرسش‌ها و تأیید خلاصه |
| `/requests/[id]` | مرحله و مهلت، خلاصه، پیشنهادها، منتخب و دسترسی به پرونده |
| `/requests/[id]/agreement` | اقلام، نسخهٔ فعال، اختلاف نسخه‌ها، تأیید، رد و انصراف |
| `/requests/[id]/parts` | نیاز قطعه، مشخصات، لینک ترب، ثبت لینک و اطلاعات دستی |
| `/requests/[id]/completion` | مخارج، تأیید/رد رسیدها، مقایسهٔ قیمت قطعه، مبلغ توافق و صورت‌حساب، نتیجه و ارزیابی |
| `/requests/[id]/dispute` | اقلام مورد اختلاف، اظهارات و شواهد دو طرف، پیشنهاد حل اختلاف، مهلت و حکم لازم‌الاجرا |
| `/requests/[id]/refund` | درخواست رایگان، دلیل و وضعیت بررسی و انتقال آزمایشی |
| `/specialist` | درخواست‌های مرتبط، پیشنهادهای خود و پرونده‌های منتخب |
| `/support` | صف اعتراض، بازپرداخت، تکمیل شواهد داوری، کیفیت منابع قیمت قطعه و تنظیمات نمایشی |

نمای چت در همان پرونده و متناسب با مرحله قرار گیرد. رابط موبایل از عرض ۳۶۰ پیکسل بدون پیمایش افقی ناخواسته کار کند؛ در دسکتاپ مقایسهٔ جدولی و در موبایل کارت با جزئیات بازشونده باشد. زمینه روشن، متن تیره، تأکید قرمز و قلم سیستمی خوانای فارسی استفاده شود؛ وابستگی به CDN فونت لازم نیست. مبلغ و شروط پشت چت پنهان نشوند. وضعیت خطا با متن و نه فقط رنگ، فوکوس صفحه‌کلید و برچسب فرم فراهم باشد.

## ۳. زمان‌ها، توافق و دسترسی

### درخواست و پیشنهاد

- انتشار در زمان سرور `publishedAt` انجام و `responseDeadline = publishedAt + 24h` ثبت شود. MVP مهلت را خودکار تمدید نمی‌کند. تمام زمان‌ها در ذخیره‌سازی UTC و در رابط با منطقهٔ زمانی تهران نمایش داده شوند.
- بازهٔ مراجعهٔ مشتری از پایان مهلت پیشنهاد آغاز شود و حداکثر ۷ روز باشد. متخصص یک زمان در این بازه پیشنهاد دهد. درخواست فوری خارج از پوشش است؛ انتخاب پیشنهاد پیش از پایان ۲۴ ساعت ممکن است.
- پیشنهاد معتبر به نسخهٔ دامنهٔ درخواست و خدمت مربوط است، فیلدهای اجباری دارد، زمان انجام در بازه است و متخصص فعال و مرتبط است. حداقل یک مبلغ قطعی، هزینهٔ معلوم عیب‌یابی یا سناریوی مشروط با مبلغ معلوم لازم است؛ هزینهٔ پایهٔ کاملاً نامعلوم پیشنهاد معتبر نیست. اعتبار پیشنهاد حداقل تا ۲۴ ساعت پس از `responseDeadline` باشد. نقص ساختاری مانع ارسال است؛ کم یا زیاد بودن مبلغ مانع نیست.
- نوع پیشنهاد مجاز با فیلد `allowedOfferTypes` در خلاصه به تأیید مشتری برسد: برای تعویض مشخص، قطعی یا مشروطِ همان عملیات؛ برای مشکل نامشخص، عیب‌یابی. عیب‌یابی ارزان، پیشنهاد معتبر یا منصفانهٔ جایگزین برای درخواست تعمیر کامل نیست. ابهام نوع خدمت پیش از انتشار با فرم رفع شود.
- پس از انتشار، تغییر اساسی خودرو، خدمت یا شرح مبنای پیشنهاد، درخواست تازه می‌خواهد. اصلاح غلط نوشتاری فقط یک یادداشت همراه با رویداد باشد و نسخهٔ دامنهٔ مبنای پیشنهادها را عوض نکند. شرایط بازپرداخت درخواست قبلی مستقل باقی می‌ماند.
- انتخاب در تراکنش و فقط برای یک متخصص انجام شود. مهلت پذیرش انتخاب، کمینهٔ زمان انتخاب + ۲ ساعت، پایان اعتبار پیشنهاد و زمان مراجعه است. رد یا انقضا درخواست را با همان مهلت اصلی به مقایسه برگرداند و آن پیشنهاد را غیرقابل انتخاب کند.
- پایان دریافت پیشنهاد، زمان پذیرش دوطرفهٔ انتخاب یا `responseDeadline`، هرکدام زودتر است. پس از آن پیشنهاد تازه دریافت نشود. تا پایان اعتبار، انتخاب از پیشنهادهای باقی‌مانده ممکن است.
- ویرایش یا پس‌گرفتن پیشنهاد پیش از انتخاب مجاز و نسخه‌دار است؛ در زمان انتظار پذیرش، متخصص فقط انتخاب را بپذیرد یا رد کند. افزایش بعدی از مسیر تغییر توافق ثبت شود.
- `scheduledAt` هنگام ثبت، انتخاب و پذیرش باید در آینده و داخل بازهٔ مراجعه باشد؛ در مرز زمان مراجعه، پیشنهاد انتخاب‌نشده و انتخاب در انتظار پذیرش دیگر قابل پذیرش نیستند، حتی اگر اعتبار قیمتی باقی باشد. سابقه و تصویر ثابت بررسی بازپرداخت حفظ شوند. پس از پذیرش همکاری، گذشتن نوبت توافق را خودکار لغو نمی‌کند؛ قبل از شروع، نوبت تازه باید در نسخهٔ توافق به تأیید دو طرف برسد. شروع خدمت فقط در نوبت جاری تأییدشده ثبت شود؛ شروع دیرتر در همان نوبت با ثبت حضور و تأیید دو طرف ممکن است. تغییر نوبت، پیشنهادگیری یا سهمیه را بازنشانی نکند.

### توافق و لغو

هر نسخهٔ توافق شامل اقلام، مبلغ کل، مبلغ قابل پرداخت به متخصص، سناریوهای مشروط، مسئول خرید، زمان، ضمانت، شروط انصراف و شرط داوری لازم‌الاجرای AI است. تغییر شامل دلیل، تفاوت نسخه‌ها و شناسهٔ مدارک است. اولین تأیید، نسخه را قفل می‌کند؛ ویرایش پیش‌نویس نسخهٔ تازه‌ای می‌سازد که تأییدهای پیش‌نویس قبلی به آن منتقل نمی‌شوند. نسخهٔ فعال قبلی و تأییدهایش تا دو تأیید نسخهٔ جدید حفظ می‌شوند. انقضای پیشنهاد، توافق فعال قبلاً پذیرفته‌شده را لغو نمی‌کند. نخستین توافق به نسخهٔ پیشنهاد منتخب متصل باشد؛ هر افزایش از آن مبنا نیز دلیل و رویداد تغییر قیمت داشته باشد و از ارزیابی جا نماند.

- عیب‌یابی فقط مجوز بررسی تعریف‌شده است. بازکردن، تعمیر و تعویض هرکدام باید در دامنهٔ پذیرفته‌شده باشند.
- شروط پیش‌فرض انصراف: پیش از شروع خدمت هزینهٔ انصراف صفر؛ پس از شروع فقط عیب‌یابی یا عملیات تکمیل‌شدهٔ از قبل پذیرفته‌شده قابل مطالبه‌اند. هزینهٔ دیگر باید قبل از ایجاد تعهد به تأیید طرفین برسد. پلتفرم در MVP وجه تعمیر را وصول نمی‌کند.
- مشتری یا متخصص می‌تواند انصراف را ثبت کند؛ قطع رابطه نیازمند رضایت طرف مقابل نیست. اختلاف دربارهٔ کار انجام‌شده با مبلغ مورد ادعا ثبت می‌شود؛ خود ادعا بدهی قطعی یا پرداخت ایجاد نمی‌کند و مبلغ نهایی از توافق حل اختلاف یا حکم داوری به دست می‌آید.
- تعویض متخصص حداکثر یک بار و فقط پیش از شروع کار در MVP ممکن است. انتخاب و توافق قبلی پایان می‌یابند، دسترسی چت قبلی قطع می‌شود و متخصص پیشین فقط سوابق متعلق به همکاری خودش را می‌بیند. سهمیهٔ باقی‌مانده منتقل می‌شود؛ بستهٔ رایگان تازه ایجاد نشود.
- اگر پیشنهاد معتبر باقی مانده باشد مشتری از همان‌ها انتخاب می‌کند؛ در غیر این صورت پرونده با علت انصراف بسته و برای خدمت تازه درخواست جدید ساخته شود. هیچ تمدید پنهان یا پیشنهادگیری نامحدود در پروندهٔ قبلی انجام نشود. پس از شروع کار، تعویض با بستن پرونده و درخواست جدید است.
- تأیید عادی صورت‌حساب فقط در صورت انطباق با توافق فعال و تأیید رسیدهای مبنا توسط مشتری ممکن است؛ هزینهٔ اضافی ابتدا تغییر توافق می‌خواهد. اگر رسید یا هزینه مورد اختلاف است، مسیر داوری به‌جای اجبار مشتری به تأیید باز شود. حکم دربارهٔ هزینه، رسید ردشده را به رسید تأییدشده توسط مشتری تبدیل نکند.

### حل اختلاف و حکم لازم‌الاجرای AI

پیش از پذیرش همکاری، شرط داوری برای مشتری و متخصص نمایش و پذیرش آن با نسخهٔ سیاست ثبت شود. هر طرف می‌تواند اختلافِ مربوط به همکاری پذیرفته‌شده را با اقلام، مبلغ مورد ادعا، دلیل و شواهد ثبت کند؛ طرف مقابل داخل پرونده مطلع شود. دامنهٔ داوری MVP، دامنهٔ کار توافق‌شده، انجام عملیات، اقلام و مبلغ صورت‌حساب و هزینهٔ انصراف همین پرونده است.

- **توافق مستقیم:** در `dispute_open`، تا ۲۴ ساعت از ثبت اختلاف فرصت اظهارنظر و ارائهٔ پیشنهاد حل اختلاف وجود دارد. این مهلت سیاست اولیهٔ قابل تنظیم است. هر طرف یک بستهٔ ساختاریافتهٔ اظهارات دارد که تا قفل ورودی قابل ویرایش است؛ هر دو با اعلام پایان اظهارات می‌توانند زودتر وارد داوری شوند. پیشنهاد حل اختلاف فقط با دو تأیید همان نسخه اعمال می‌شود و سکوت، تأیید نیست.
- **ورود به داوری:** اگر توافق حاصل نشد، پس از پایان فرصت یا اعلام پایان اظهارات هر دو طرف، snapshot توافق‌ها، رسیدها و وضعیت تأییدشان، شواهد متنی، اظهارات و اقلام مورد اختلاف قفل شود. AI فقط همین ورودی را بررسی کند. بی‌پاسخی طرف مقابل ثبت می‌شود و مانع رسیدگی نیست، ولی ادعای طرف دیگر را خودکار اثبات نمی‌کند.
- **خروجی و اجرا:** حکم برای هر قلم مورد اختلاف، پذیرش/رد کامل یا جزئی، مقدار و مبلغ مستند، دلیل، شناسهٔ شواهد و مبنای تصمیم بدهد. مبلغ نهایی را کد از جمع اقلام انجام‌شدهٔ بدون اختلاف و اقلام پذیرفته‌شده در حکم محاسبه کند؛ مبلغ کل خدمت و مبلغ قابل پرداخت به متخصص جدا بمانند و قطعهٔ مشتری دوباره به حساب متخصص اضافه نشود. حکم حق ساخت نرخ، قطعه یا عملیات بی‌منبع ندارد. هزینهٔ اضافهٔ فاقد شاهد یا مجوز لازم صرفاً با ادعا پذیرفته نشود. `DisputeDecision` و نتیجهٔ تسویه پس از اعتبارسنجی و کنترل نسخه در یک تراکنش، دقیقاً یک بار اعمال و به طرفین ابلاغ شوند؛ تأیید مجدد هیچ‌کدام شرط اجرا نیست. توافق و تأییدهای قدیمی بازنویسی نشوند.
- **پایان پرونده:** توافق مستقیم به `resolved_by_agreement` و حکم اجراشده به `resolved_by_ai` برسد؛ درخواست با `closed_settled` یا `closed_adjudicated` بسته شود. انجام‌شدن کار و مبلغ تسویه مستقل ثبت شوند؛ بسته‌شدن با حکم، رضایت مشتری یا تعمیر موفق فرض نشود. اجرای حکم در MVP ثبت نتیجه و مبلغ لازم‌الاجرا در سامانه است؛ وصول وجه تعمیر و پرداخت واقعی جزو MVP نیست.
- **اطلاعات ناکافی یا خطا:** AI می‌تواند یک بار فهرست دقیق اطلاعات لازم را با `needs_evidence` برگرداند؛ دو طرف و پشتیبان تا ۴۸ ساعت برای تکمیل همان پرونده فرصت دارند. پس از آن، داوری با شواهد موجود و ثبت موارد اثبات‌نشده تکرار شود. اگر خروجی معتبر قابل صدور نبود یا مدل قطع بود، `awaiting_ai` ثبت و برای رفع نقص فنی/داده‌ای ارجاع شود؛ پایان مهلت یا بی‌پاسخی، حکم جعلی و پذیرش هزینه ایجاد نکند. پشتیبان شواهد را تکمیل می‌کند و جانشین صادرکنندهٔ حکم نیست. سقف تلاش و بودجهٔ داوری طبق بخش ۷ حفظ شود.

در MVP برای هر پرونده یک روند داوری با تکمیل شواهدِ محدود وجود دارد؛ ارسال تکراری همان اختلاف، حکم یا سهمیهٔ تازه نسازد. اعتراض به امتیاز یا بازپرداخت، حکم تسویه را خودکار تغییر نمی‌دهد. تا قبل از اعمال حکم، توافق مستقیم همچنان ممکن است؛ پذیرش آن snapshot داوری را منقضی کند و پاسخ دیررس AI اعمال نشود.

### مجوزها

مشتری فقط پرونده‌های خود و پیشنهادهای آن‌ها را می‌بیند. متخصص مرتبط پیش از انتخاب فقط خلاصهٔ فنی، محله و بازهٔ زمانی و پیشنهاد خودش را می‌بیند؛ پیشنهاد رقبا، تماس، مدارک خصوصی و چت دیگران در دسترس او نیست. پس از پذیرش انتخاب، اطلاعات لازم مراجعه و مدارک مجاز همان همکاری در دسترس منتخب است. اختلاف به شناسهٔ همان همکاری متصل باشد؛ متخصصِ همکاری پایان‌یافته فقط برای اختلاف خودش حق مشاهده و ارائهٔ اظهارات دارد و چت اجرایی یا مدارک همکاری تازه برای او باز نشود. پشتیبان به پروندهٔ ارجاع‌شده و سابقهٔ اقدام دسترسی دارد؛ تغییر نتیجهٔ ارزیابی امتیاز یا بازپرداخت دلیل و رویداد حسابرسی می‌خواهد. حکم تسویهٔ AI توسط پشتیبان بازنویسی نشود. شناسهٔ نقش و مالکیت از نشست سمت سرور خوانده شوند، نه ورودی فرم.

## ۴. قیمت، مرجع بازار و قطعه

### اقلام و مقایسه

سه نوع پیشنهاد مستقل‌اند: `fixed` برای کار روشن، `conditional` با سناریو و شرط قابل بررسی، و `diagnostic` با هزینه و دامنهٔ عیب‌یابی. هیچ قیمت عیب‌یابی به‌عنوان تعمیر کامل نمایش داده نشود. در پیشنهاد مشروط، مقایسه به تفکیک سناریوی مشترک باشد؛ مبلغ بدون سناریو وارد رتبه‌بندی قیمت نشود.

هر ردیف شامل نوع `part/labor/extra`، عنوان، مشخصات، تعداد یا زمان، نرخ، مسئول تأمین و پرداخت و وضعیت معلوم‌بودن مبلغ است. مبلغ نامعلوم `null` است و صفر نیست. اجرت ردیف برابر زمان توافقی به دقیقه × نرخ ساعتی ÷ ۶۰، با گردکردن به نزدیک‌ترین تومان است. زمان مرجع از قالب عملیات و زمان واقعی از گزارش متخصص می‌آید؛ زمان بیشتر خودکار اجرت بیشتر نمی‌سازد. عملیات مشترک شناسهٔ یکتا داشته باشند تا باز و بسته‌کردن دوباره شمرده نشود.

مبالغ، اعداد صحیح تومان‌اند و جمع و اعتبارسنجی با نرم‌افزار انجام می‌شوند. هزینهٔ کل خدمت شامل قطعهٔ خریده‌شده توسط مشتری نیز هست؛ «قابل پرداخت به متخصص» آن را دوباره شامل نمی‌شود. قیمت فروشنده، قیمت توافق و مبلغ رسید جدا ذخیره شوند. ارسال، رفت‌وآمد خرید، ضمانت قطعه و اجرت و کسر هزینهٔ عیب‌یابی از تعمیر صریح باشند.

ترتیب پیش‌فرض در هر گروه قابل مقایسه: قیمت کل معلوم صعودی؛ در تساوی قیمت، امتیازهای دارای سابقهٔ کافی نزولی و سپس موارد بدون امتیاز؛ در تساوی بعدی زمان انجام زودتر و سپس شناسهٔ پیشنهاد. پیشنهاد با هزینهٔ نامعلوم در انتهای گروه و با توضیح بیاید. در مرتب‌سازی مستقل براساس زمان، کلید دوم قیمت و سپس شناسه باشد؛ در مرتب‌سازی براساس امتیاز، امتیاز معلوم نزولی، موارد نامعلوم در انتها، سپس قیمت و زمان و شناسه. هر کلید نامعلوم در جایگاه خودش آخر قرار گیرد؛ امتیاز نامعلوم صفر تلقی نشود و ترتیب دو پیشنهاد با مقایسهٔ دوباره عوض نشود. AI رتبه‌بندی پنهان تازه‌ای ایجاد نکند.

### ساخت مرجع قیمت

مرجع از مبالغ نهایی تعمیرهای انجام‌شده با تفکیک قطعه و اجرت ساخته می‌شود. هم‌گروهی اولیه دقیق است: شهر، مدل و مشخصات فنی خودرو، عملیات و سناریو، مشخصات و وضعیت قطعه و سطح ضمانت. مسئول خرید در مبلغ کل هم‌مبنا شود؛ نام محله به‌تنهایی گروه تازه نسازد. دادهٔ ۶۰ روز اخیر استفاده شود. قیمت پیشنهادی انجام‌نشده و مبلغ حافظهٔ مدل، مرجع نیستند.

کیفیت `self_reported`، `documented` و `verified` جدا ذخیره شود. ورود به مرجع معتبر نیازمند پایان تأییدشدهٔ طرفین، ریز اقلام، مدرک هزینه، رضایت برای استفادهٔ بی‌نام و تأیید پشتیبان است. پروندهٔ دارای اختلاف باز، رسید تکراری و دادهٔ متخصصِ در حال ارزیابی از مرجع همان بررسی کنار گذاشته شوند. مبلغ تعیین‌شده با حکم AI بدون این تأییدهای مستقل خودکار وارد مرجع بازار نشود. هیچ داده‌ای فقط به‌علت فاصلهٔ قیمتی حذف نشود؛ پشتیبان علت رد کیفیت را ثبت کند.

آمادگی یک گروه یعنی حداقل ۱۰ پروندهٔ معتبر از حداقل ۳ متخصص مستقل در ۶۰ روز، با سهم حداکثر ۴۰٪ از یک متخصص، پس از حذف موارد بالا. میانگین حسابی، تعداد، کمینه، بیشینه، تاریخ و شناسهٔ سوابق ذخیره شوند. نمونه‌های نمایشی هرگز گروه واقعی را آماده نمی‌کنند؛ در دمو پرچم مستقل `demo_reference_ready` فقط سناریوی سیاست مبتنی بر داده را فعال می‌کند.

برای بررسی انصاف، آستانهٔ اولیهٔ قابل تنظیم `fairPriceFactor = 1.20` است: مبلغ کل هم‌دامنه تا ۲۰٪ بالاتر از میانگین، از نظر عددی قابل قبول است. AI هم‌ارزی دامنه و شواهد تفاوت را ارزیابی می‌کند؛ اگر هم‌ارزی قابل اثبات نباشد نتیجه نامعلوم است. مدل مجاز به ساخت قیمت جبرانی یا تغییر آستانه نیست. این معیار فقط برای بررسی بازپرداخت است و قیمت پیشنهاد یا توافق را محدود نمی‌کند. آستانه و حداقل نمونه، سیاست اولیهٔ قابل کالیبراسیون‌اند و به‌عنوان یافتهٔ بازار معرفی نشوند.

### قطعات و مدارک

صفحهٔ قطعه، مشخصات خودرو و قطعه را از پرونده می‌خواند و اگر لازم باشد کد قطعه یا سال خودرو را از متخصص می‌گیرد. دکمهٔ جست‌وجو لینک `https://torob.com/search/?query=...` با عبارت کدگذاری‌شده باز می‌کند. کاربر می‌تواند لینک محصول، فروشنده، قیمت، برند، شرایط تحویل و زمان مشاهده را دستی ثبت کند. فقط لینک HTTPS با میزبان دقیق `torob.com` یا `www.torob.com` پذیرفته شود؛ سرور لینک کاربر را واکشی نمی‌کند.

نتیجهٔ دستی با منشأ «واردشده توسط کاربر؛ تأیید زنده نشده» نمایش داده شود. انتخاب قطعه نیازمند تأیید مشخصات توسط متخصص است و خرید نزد فروشنده با اقدام صریح شخص انجام می‌شود. فاصله، موجودی و تحویل نامعلوم حدس زده نشوند. مقایسهٔ نمونه براساس سازگاری تأییدشده، هزینهٔ کل معلوم و امکان تحویل اعلام‌شده باشد؛ نزدیکی فقط در حد شهر/محلهٔ معلوم نمایش داده شود.

مخارج با متن یا فرم ثبت و AI اقلام متن را استخراج می‌کند. متخصص متن استخراج‌شده را بازبینی و سپس مشتری تصویر رسید و ریز اقلام همان نسخه را تأیید یا با دلیل رد کند؛ امکان تأیید گروهی با نمایش تمام اقلام و اقدام صریح وجود داشته باشد. هر ویرایش مبلغ، اقلام یا فایل، نسخهٔ تازه می‌سازد و تأیید قبلی را به آن منتقل نمی‌کند. وضعیت `pending/confirmed/rejected` و عامل و زمان تأیید ثبت شوند. تأیید رسید به‌تنهایی مجوز افزایش مبلغ توافق نیست؛ رد آن مسیر اصلاح یا اختلاف دارد.

حداکثر ۳ تصویر JPEG/PNG، هرکدام ۲ مگابایت، برای مدرک دستی پذیرفته شود؛ OCR و ارسال تصویر به مدل در MVP انجام نشود. نوع واقعی فایل بررسی، نام ذخیره تصادفی و فایل خارج از مسیر عمومی نگهداری شود. مشتری و پشتیبان تصویر را می‌بینند؛ مدل فقط اقلام متنی و وضعیت تأیید را دریافت می‌کند. اصالت‌سنجی توسط AI الزامی نیست و تأیید مشتری نیز با اعتبارسنجی منبع بازار یکسان ثبت نشود.

### اختلاف قیمت فاکتور قطعه با ترب

برای قطعه‌ای که متخصص تأمین کرده یا قیمتش را به مشتری صورتحساب می‌کند، قیمت واحد فاکتور با قیمت همان قطعه در ترب مقایسه شود. قطعهٔ خریده‌شده مستقیماً توسط مشتری امتیاز منفی برای متخصص ایجاد نکند. کد قطعه و سازگاری، برند، نو/کارکرده‌بودن، تعداد، ضمانت، شرایط پرداخت و هزینهٔ تحویل باید هم‌مبنا باشند؛ اجرت نصب و هزینهٔ خریدِ جداگانه و توافق‌شده با قیمت خود قطعه مخلوط نشوند. کاهش قیمت فاکتور نسبت به ترب اثر منفی ندارد.

**فرض اجرایی اولیه و قابل تنظیم:** `invoiceOverpriceFactor=1.30`؛ قیمت هم‌مبنای فاکتور بیش از ۳۰٪ بالاتر از میانهٔ قیمت حداقل ۳ فروشندهٔ مستقل ترب، اختلاف فاحش محسوب شود. قیمت‌ها در فاصلهٔ حداکثر ۲۴ ساعت پیش از خرید ثبت شده، امکان خرید با همان شرایط داشته و منبعشان تأیید شده باشد. مقدار ۳۰٪ و حداقل نمونه، سیاست پیشنهادی اجرای MVP هستند، نه عدد تعیین‌شده توسط کاربر یا نتیجهٔ تحقیق بازار؛ از `fairPriceFactor` بررسی بازپرداخت مستقل بمانند.

در MVP لینک و قیمت ترب دستی وارد می‌شوند. `PartPriceSnapshot` شامل لینک، فروشنده، مشخصات، مبلغ، زمان مشاهده و خرید، شرایط، مدرک مشاهده و تأیید پشتیبانِ منبع باشد؛ صرف واردکردن عدد توسط یک طرف برای کسر امتیاز کافی نیست. قیمت پس از خرید جای قیمت زمان خرید ننشیند. دادهٔ نمونه در دمو مشخص باشد و بررسی خودکار یا اتصال زندهٔ ترب ادعا نشود. اگر دادهٔ کافی یا هم‌ارزی قابل اتکا نیست، «قابل بررسی نیست» نمایش داده شود و امتیاز منفی ساخته نشود.

مبنای ثبت اثر منفی، فاکتور تأییدشده توسط مشتری یا قلمی است که مبلغش در داوری احراز شده؛ روی ریز اقلامِ هنوز محل اختلاف، هشدار «نیازمند بررسی» نشان داده شود. در اختلاف فاحشِ احرازشده، هشدار داخل پرونده با مقدار اختلاف، مبنا و لینک‌ها به مشتری نشان داده و رخداد `invoice_overprice` برای امتیاز متخصص ثبت شود؛ تأیید رسید توسط مشتری این رخداد را حذف نکند. همان رخداد با تکرار بررسی دوباره شمرده نشود. اعتراض متخصص طبق بخش ۵ بررسی و اصلاح منبع یا هم‌ارزی، نتیجهٔ نسخه‌دار تازه بسازد. این بررسی به‌تنهایی مبلغ توافق، حکم داوری یا بازپرداخت هزینهٔ ثبت درخواست را تغییر نمی‌دهد.

## ۵. امتیاز عملکرد و رسیدگی به اعتراض

AI دلیل افزایش را در تمام زنجیرهٔ نسخهٔ پیشنهاد منتخب، اولین توافق، تغییرات بعدی و صورت‌حساب بررسی می‌کند و برای هر تغییر یکی از `justified`، `unjustified` یا `insufficient_evidence` می‌دهد. افزایش بی‌دلیل از پیشنهاد منتخب تا نخستین توافق نیز اثر منفی دارد؛ ثبت آن به‌عنوان «اولین توافق» استثنا نیست. خروجی شامل دلیل کوتاه، شناسهٔ شواهد و دادهٔ مفقود است. موافقت مشتری اجازهٔ تغییر است و دلیل موجه‌بودن آن محسوب نمی‌شود. شواهد ناکافی برچسب تقلب نیست. رخداد مستند `invoice_overprice` نیز علت مستقل اثر منفی بر شفافیت هزینه است.

قیمت آزاد، پیشنهاد بالاتر از مرجع، پایین‌آوردن قیمت و بازپرداخت ثبت درخواست به‌تنهایی اثر منفی ندارند. افزودن کار تازه با مدرک و توافق با افزایش بی‌دلیل همان کار فرق دارد. هیچ جریمهٔ مالی، محرومیت یا تعلیق مبتنی بر این امتیاز پیاده نشود.

فرمول اولیهٔ «شفافیت هزینه»:

- جامعه، حداکثر ۵۰ پروندهٔ بسته‌شده و قابل ارزیابیِ اخیر در ۱۸۰ روز است؛ پایان عادی، حل اختلاف و لغوِ دارای رفتار منفی مستند را شامل می‌شود. هر پرونده فقط یک نمونه است، حتی اگر چند علت منفی داشته باشد.
- پرونده با حداقل یک افزایش ناموجه یا یک اختلاف فاحش قیمت فاکتورِ احرازشده، یک نمونهٔ منفی است. پروندهٔ تکمیل‌شدهٔ بدون علت منفی و با اقلام قابل ارزیابی، نمونهٔ مثبت است. پروندهٔ صرفاً نامعلوم از مخرج کنار گذاشته شود؛ وجود علت منفیِ احرازشده با یک قلم دیگرِ نامعلوم، اثر منفی را حذف نکند. لغو یا انجام‌نشدن کار نمونهٔ مثبت نمی‌سازد و رفتار منفی مستند را هم پاک نمی‌کند.
- با `N` پروندهٔ قابل ارزیابی و `U` پروندهٔ دارای حداقل یک علت منفی، امتیاز برابر `round(100 × (1 − U/N))` است. پیش از ۵ پرونده، به‌جای عدد «سابقهٔ کافی نیست» و تعداد نمونه نمایش داده شود؛ رخداد منفی از همان بار اول ثبت می‌شود.
- تعداد پرونده، دوره، دلیل و نسبت پرونده‌های منفی کنار امتیاز نمایش داده شوند؛ علت افزایش ناموجه و قیمت فاکتور جدا قابل مشاهده باشند. کیفیت فنی تعمیر از این عدد نتیجه گرفته نشود. رضایت مشتری جدا، با امتیاز اختیاری ۱ تا ۵ و تعداد پاسخ‌ها نمایش داده شود.
- ثبت هشدار فاکتور هنگام احراز انجام شود؛ تجمیع امتیاز پس از بستن پرونده با پایان عادی، توافق حل اختلاف، حکم یا لغو انجام شود. بسته‌شدن با داوری، تأیید مشتری یا سابقهٔ تعمیر موفق جعل نکند.

متخصص تا ۷ روز پس از ابلاغ نتیجه می‌تواند یک اعتراض و یک مدرک تکمیلی ثبت کند. پروندهٔ مورد اعتراض موقتاً از تجمیع امتیاز کنار گذاشته شود و وضعیت بازبینی دیده شود. پشتیبان ظرف ۴۸ ساعت شواهد را اصلاح یا ارزیابی را با ورودی تازه تکرار می‌کند؛ تغییر دستی نتیجه نیازمند دلیل و ثبت عامل است. اگر نتیجه تا مهلت قابل اتکا نشد، `insufficient_evidence` ثبت شود. سابقهٔ نتیجهٔ قبلی حذف نشود. هزینهٔ این رسیدگی سهمیهٔ مشتری را مصرف نکند.

## ۶. بازپرداخت ثبت درخواست

مبلغ واجد بازپرداخت کل هزینهٔ بستهٔ همین پرونده است؛ اجرت، قطعه، عیب‌یابی و اشتراک احتمالی متخصص را شامل نمی‌شود. درخواست بررسی رایگان است و به سهمیهٔ چت وابسته نیست. در MVP برگشت وجه شبیه‌سازی می‌شود و رسید صریحاً آزمایشی است.

### نسخهٔ سیاست و ترتیب تصمیم

سیاست و مبلغ در زمان پرداخت ذخیره شوند. اگر گروه درخواست مرجع کافی ندارد یا گروه دقیق هنوز نامعلوم است، حالت `bootstrap` ثبت شود؛ این حق با افزایش بعدی داده محدود نشود. تغییر دامنه‌ای که باعث کمبود مرجع می‌شود فقط می‌تواند حق بازپرداخت را به نفع مشتری گسترش دهد. در حالت پیش‌فرض دمو، دادهٔ واقعی آماده نیست و `bootstrap` است.

1. **بدون پیشنهاد معتبر:** اگر تا پایان مهلت ۲۴ساعته پیشنهاد معتبر وجود نداشته و انتخاب موفقی هم انجام نشده، استحقاق بازپرداخت خودکار ثبت شود؛ به تحلیل قیمت AI نیاز نیست. این مسیر در هر دو حالت سیاست برقرار است.
2. **درخواست در حالت bootstrap:** درخواست مشتری با کنترل مالکیت پرداخت و نبود استرداد قبلی پذیرفته شود؛ انتظار ۲۴ساعته، اثبات گرانی یا تأیید AI لازم نیست. وجود پیشنهاد منصفانه مانع نیست. این قاعده، استرداد عمومی همهٔ پرونده‌ها بدون درخواست مشتری ایجاد نمی‌کند.
3. **اعتراض قیمتی با مرجع آماده:** با سوابق مشابه انجام‌شده بررسی شود. وجود حتی یک پیشنهاد معتبر و منصفانه، بازپرداخت قیمتی را منتفی می‌کند؛ انتخاب‌نکردن آن یا گرانی بقیه اثری ندارد. نبود چنین پیشنهاد و احراز اختلاف به بازپرداخت منتهی می‌شود.

### زمان و دادهٔ بررسی

در سیاست دارای مرجع، مشتری از رسیدن اولین پیشنهاد تا ۷ روز پس از پایان مهلت پیشنهاد یا پذیرش انتخاب، هرکدام دیرتر است، می‌تواند اعتراض قیمتی ثبت کند. بررسی نهایی از زمان پایان دریافت پیشنهاد آغاز شود؛ درخواست زودتر در صف می‌ماند. درخواست bootstrap با این مهلت قیمتی محدود نشود.

در زمان پایان دریافت پیشنهاد، تصویر ثابتی از نسخه‌های معتبر و قابل انتخاب ذخیره شود. پیشنهادی که پیش از این زمان پس گرفته یا رد شده، یا هرگز قابل انجام نبوده، مانع بازپرداخت نیست. انقضای طبیعی بعدیِ پیشنهادی که مشتری فرصت معتبر انتخاب آن را داشته، به‌تنهایی استحقاق تازه نمی‌سازد. اگر پس از انتخاب، متخصص توان انجام را رد کند، اعتبار همان پیشنهاد برای بررسی بازپرداخت هم دوباره سنجیده شود؛ سایر پیشنهادهای معتبر محفوظ‌اند. انصراف دلخواه مشتری خودکار پیشنهاد معتبر را نامعتبر نمی‌کند.

نرم‌افزار اقلام و مبلغ هم‌دامنه و میانگین را محاسبه می‌کند؛ AI تشابه و شواهد را تأیید و نتیجهٔ هر پیشنهاد را `fair/unfair/insufficient_evidence` اعلام می‌کند. قاعدهٔ عددی بخش ۴ در همین بررسی استفاده شود. در صورت یک `fair` مستند، رد قیمتی ممکن است. تأیید به علت گرانی فقط وقتی مجاز است که همهٔ پیشنهادهای معتبر بررسی و هیچ‌کدام منصفانه نباشند. تعداد زیاد پیشنهاد یا سقف مصرف مجوز نادیده‌گرفتن پیشنهادهای بررسی‌نشده نیست.

برای کنترل هزینه، ابتدا پیشنهادهای ارزان‌تر و کامل‌تر به‌صورت دسته‌ای بررسی شوند؛ مدل برای هر پیشنهاد جدا صدا زده نشود. نبود نمونهٔ کافی، اختلاف در هم‌ارزی، خروجی نامعتبر یا قطع AI به صف پشتیبان می‌رود. مهلت رسیدگی ۴۸ ساعت از شروع بررسی نهایی است. اگر تا آن زمان هیچ پیشنهاد منصفانه‌ای با شواهد احراز نشد و بررسی همچنان نامعلوم بود، بازپرداخت با دلیل «تکمیل‌نشدن بررسی در مهلت» پذیرفته شود؛ این تصمیم اثبات گران‌فروشی نیست و امتیاز متخصص را کاهش نمی‌دهد.

### انتقال، تکرار و اعتراض به نتیجه

استحقاق و انتقال جدا باشند: `requested → reviewing` و سپس یکی از `approved` یا `rejected`. فقط مسیر تأییدشده به `approved → transfer_pending → paid/transfer_failed` می‌رود؛ تلاش مجدد مجاز از `transfer_failed` به `transfer_pending` است. `rejected` انتقال وجه ندارد و بازبینی مجاز آن مطابق بند بعد انجام می‌شود. حالت `needs_review` زیرمجموعهٔ بررسی با مهلت مشخص است. هر پرداخت فقط یک بازپرداخت کامل دارد؛ شناسهٔ یکتا و تراکنش جلوی استرداد دوباره را بگیرند. تلاش مجدد انتقال همان شناسه را استفاده کند. مجموع استرداد از مبلغ پرداخت‌شده بیشتر نشود.

اعتراض تکراری با همان ورودی همان نتیجه را نشان دهد. مشتری تا ۷ روز پس از رد، یک بازبینی با مدرک تازه درخواست کند؛ بیشتر از آن برای پشتیبان ثبت شود و فراخوانی خودکار نامحدود نسازد. بازبینی نیز رایگان و دارای مهلت ۴۸ ساعت است. اگر پرداخت اصلی موفق نشده، بازپرداخت ساخته نشود. رهاشدن درخواست پیش از انتشار، با درخواست مشتری هزینهٔ بسته را بازمی‌گرداند و پرونده را می‌بندد. بازپرداختِ پروندهٔ دارای همکاری جاری، توافق را خودکار لغو یا سهمیه را بازنشانی نمی‌کند؛ ادامه با همان سهمیهٔ باقی‌مانده ممکن است.

## ۷. AI و مصرف قابل پیش‌بینی

### ارائه‌دهنده و مرز اختیار

دو پیاده‌سازی برای قرارداد `AiProvider` بساز: `MockAiProvider` و `DeepSeekProvider`. قرارداد و مدل‌های Pydantic متعلق به برنامه باشند؛ اشیای LangChain به API، دامنه یا مدل‌های پایگاه نشت نکنند. حالت پیش‌فرض `AI_MODE=mock` است؛ پاسخ‌ها از سناریوی نمونه و قواعد محدود تولید و با برچسب «پاسخ نمایشی» نمایش داده شوند. mock کلید، اتصال شبکه یا اجرای مدل نیاز ندارد. ورودی نامشخص به فرم عمومی یا بررسی حضوری هدایت شود؛ پاسخ آماده به‌عنوان اجرای واقعی مدل معرفی نشود.

اتصال واقعی فقط با `AI_MODE=live`، کلید، تعرفه و سقف هزینهٔ تنظیم‌شده فعال شود. `DeepSeekProvider` از `ChatDeepSeek` در بستهٔ `langchain-deepseek` و اجزای پیام/پرامپت `langchain-core` استفاده کند؛ مدل اولیه `deepseek-flash` است. برای پرسش و استخراج، `thinking.type=disabled`؛ برای ارزیابی عملکرد، اعتراض قیمتی و داوری اختلاف، `thinking.type=enabled` و `reasoning_effort=low` صریحاً ارسال شوند. پارامتر اختصاصی thinking از `extra_body` عبور کند و به پیش‌فرض ارائه‌دهنده تکیه نشود. فراخوانی با `ainvoke` و بدون streaming انجام شود؛ client در طول عمر فرایند بازاستفاده و تنظیمات هر فراخوانی مستقل از درخواست‌های هم‌زمان باشند. مرجع اتصال و پارامترها: [ChatDeepSeek](https://docs.langchain.com/oss/python/integrations/chat/deepseek)، [Thinking Mode](https://api-docs.deepseek.com/guides/thinking_mode/).

مسیر اجرای AI چنین باشد: API یا worker، سرویس پایتونی `AiService` را فراخوانی کند؛ این سرویس مجوز، مرحله و نسخهٔ ورودی را کنترل، پیام نهایی را آماده و بودجه را رزرو کند، سپس `AiProvider` را اجرا و مصرف و نتیجه را ثبت کند. LangChain فقط برای آماده‌سازی پیام، اتصال مدل و تبدیل خروجی استفاده شود؛ وضعیت پرونده و سابقهٔ گفتگو در PostgreSQL بمانند. سه مرحلهٔ گفتگو، سه جریان از پیش تعریف‌شده‌اند؛ CrewAI، ساخت جریان LangGraph، `create_agent`، مرور وب و حلقهٔ خودمختار ابزار در MVP استفاده نشوند. [فراخوانی مستقل مدل در LangChain](https://docs.langchain.com/oss/python/langchain/models)

برای خروجی از `with_structured_output(schema, method="json_mode", include_raw=True)` با schemaهای Pydantic برنامه استفاده شود. پرامپت صریحاً JSON و ساختار لازم را درخواست کند؛ همهٔ دستورها، schema و سابقهٔ ارسالی در شمارش ورودی منظور شوند. حالت JSON به‌تنهایی تضمین انطباق با schema یا صحت تشخیص نیست. پاسخ خالی، قطع‌شده یا نامنطبق و شناسهٔ شاهد نامعتبر، خروجی قابل اعمال محسوب نشوند. `include_raw` فقط برای استخراج مصرف و وضعیت پاسخ در حافظه است؛ پیام خام یا استدلال داخلی ذخیره یا نمایش داده نشوند. [خروجی ساختاریافتهٔ ChatDeepSeek](https://reference.langchain.com/python/langchain-deepseek/chat_models/ChatDeepSeek/with_structured_output)، [قالب پاسخ DeepSeek](https://api-docs.deepseek.com/api/create-chat-completion/)

`max_retries=0` در client مدل و هر لایهٔ شبکهٔ زیر آن اعمال شود؛ timeout تابع سقف همین سند است. تکرار فنی و اصلاح قالب فقط با تصمیم `AiService` و رزرو هر تلاش انجام شوند؛ `with_retry`، اصلاح‌گر خودکارِ فراخوانندهٔ مدل و fallback خودکار ارائه‌دهنده فعال نشوند. هر تلاش آداپتر حداکثر یک درخواست مدل بفرستد. مصرف از metadata پاسخ سرویس، حتی در خطای تبدیل JSON یا اعتبارسنجی، به قرارداد داخلی برگردد؛ خروجی قابل‌صورتحساب شامل استدلال است و جزئیات آن دوباره به جمع کل اضافه نشوند. نبود usage به مصرف صفر تبدیل نشود و همان مسیر رزرو نامعلوم اجرا شود. ثبت نتیجه و مصرف در پایگاه خود برنامه باشد؛ LangSmith و tracing بیرونی به‌صورت پیش‌فرض غیرفعال و پیش‌نیاز اجرا نباشند. حالت نامعتبر یا نبود کلید خطای روشن بدهد و بی‌صدا به پاسخ ساختگی تبدیل نشود.

GPT-5.6 Luna برای مقایسهٔ هزینه و کیفیت و GPT-6 Astra و DeepSeek Pro برای مقایسهٔ موارد دشوار ثبت می‌شوند؛ ساخت اتصال آن‌ها یا مسیریابی خودکار چندمدلی جزو MVP نیست. انتخاب DeepSeek برای شروع، ادعای برتری آزموده‌شده نیست. [مدل‌های OpenAI](https://developers.openai.com/api/docs/models)

متن مشتری، متخصص و رسید دادهٔ غیرقابل اعتماد است؛ دستورهای داخل آن قواعد سیستم را عوض نکنند. AI اجازهٔ جعل تأیید طرفین، انتقال مستقیم پول، تغییر مجوز یا تولید قیمت و موجودی بی‌منبع ندارد. در مسیر عادی فقط پیشنهاد و ارزیابی می‌دهد؛ در مسیر داوریِ مجاز، خروجی معتبرش مبنای لازم‌الاجرای تسویه است، حتی اگر یک طرف هزینه را قبول نکند. اعمال حکم توسط سرویس دامنه با کنترل نقش، نسخه، اقلام و سقف مبلغ ادعاشده انجام شود؛ ابزار تغییر مستقیم پایگاه یا پرداخت در اختیار مدل نباشد. خروجی‌ها شامل دلیل کوتاه قابل ارائه و شناسهٔ شواهد باشند؛ استدلال داخلی خام ذخیره یا نمایش داده نشود. محاسبه، دسترسی و تغییر وضعیت در کد انجام شوند.

### سقف‌های اولیه

هر مرحله حداکثر ۳ نوبت موفق دارد و مسیر عادی در ۲ نوبت تمام می‌شود. یک نوبت یعنی پیام شخص و پاسخ دستیار؛ تأیید دکمه‌ای، مشاهده و فرم دستی نوبت نیستند. در مرحلهٔ متخصص و محل، سهمیه میان مشتری و متخصص منتخب مشترک است. تعویض متخصص سهمیه را بازنشانی نمی‌کند.

| سهمیهٔ کل مرحله | ورودی | خروجی قابل‌صورتحساب |
| --- | --- | --- |
| مشتری | ۸٬۰۰۰ | ۲٬۰۰۰ |
| متخصص منتخب | ۱۰٬۰۰۰ | ۲٬۰۰۰ |
| محل و پایان | ۱۰٬۰۰۰ | ۳٬۰۰۰ |
| کارهای کمکی | ۱۲٬۰۰۰ | ۳٬۰۰۰ |
| سقف پرونده | ۴۰٬۰۰۰ | ۱۰٬۰۰۰ |

ورودی آزاد هر پیام حداکثر ۱٬۰۰۰ توکن است؛ فیلد کوتاه حداکثر ۲۰۰ نویسه. سقف خروجی هر پاسخ مشتری/متخصص ۶۵۰، محل ۹۰۰ و کار کمکی ۱٬۰۰۰ توکن شامل استدلال است. سه کار کمکیِ خودکار، حداکثر یک بار توضیح مقایسه، یک بار استخراج گروهی مخارج و یک بار ارزیابی نهایی‌اند. اصلاح بیشتر دستی یا از نوبت باقی‌ماندهٔ مرحله انجام شود.

حداکثر ۱۲ فراخوانی عادی و مجموعاً ۱۶ تلاش با تکرار فنی در سقف پرونده مجاز است. هر خطای گذرا فقط یک بار پس از ۲ ثانیه تکرار شود؛ زمان انتظار هر فراخوانی ۴۵ ثانیه. خطای قالب نیز حداکثر یک تلاش اصلاحی از همین بودجه دارد. فراخوانی پرداخت‌شدهٔ ناموفق مصرف فنی دارد، ولی نوبت موفق شخص را کم نمی‌کند و شارژ تازه نمی‌سازد.

قبل از فراخوانی، ورودی و بیشینهٔ خروجی در تراکنش رزرو شوند تا درخواست هم‌زمان سقف را دور نزند. شمارش ورودی با tokenizer سازگار مدل انجام شود؛ اگر در دسترس نبود، تعداد بایت UTF-8 متن به‌علاوهٔ سربار محافظه‌کارانهٔ ۲۵۶ واحد برای هر پیام به‌عنوان رزرو اولیه استفاده و «تخمین محافظه‌کارانه» ثبت شود. این روش جای اندازه‌گیری مصرف واقعی نیست. پس از پاسخ، مصرف سرویس مبناست. در timeout با مصرف نامعلوم، رزرو تا تعیین تکلیف آزاد نشود؛ اگر مغایرت شمارش سقف را رد کرد، فراخوانی بعدی متوقف و رویداد ثبت شود.

پایان سهمیه به خلاصهٔ موجود و فرم دستی هدایت شود. توافق، مشاهدهٔ پیشنهاد و درخواست بازپرداخت همچنان فعال‌اند. ارزیابی انجام‌نشده با وضعیت «در انتظار/اطلاعات ناکافی» نمایش داده شود و امتیاز ساختگی نسازد. فروش بستهٔ اضافه در MVP پیاده نشود.

بررسی بازپرداخت یا بازبینی امتیاز برای هر پرونده بودجهٔ عملیاتی مستقل با سقف ۸٬۰۰۰ ورودی، ۲٬۰۰۰ خروجی و ۳ تلاش در هر کدام دارد؛ به مشتری صورتحساب نمی‌شود. پس از سقف یا تکرار بدون مدرک تازه، رسیدگی دستی و مهلت بخش‌های ۵ و ۶ برقرار است. بازبینی به‌دلیل تغییر مدل خودکار برای همهٔ سوابق اجرا نشود.

داوری اختلاف نیز یک بودجهٔ عملیاتی مستقل برای کل روند همان پرونده دارد: سقف ۸٬۰۰۰ ورودی، ۲٬۰۰۰ خروجی شامل استدلال و ۳ تلاش با احتساب تکمیل شواهد و retry؛ هر پاسخ حداکثر ۱٬۰۰۰ توکن. پایان سهمیهٔ چت مشتری مانع ثبت اختلاف یا دریافت حکم نیست و هزینهٔ جداگانه‌ای به او تحمیل نشود. پس از پایان این بودجه، کار خودکار متوقف و `awaiting_ai` برای رسیدگی ثبت شود؛ پشتیبان پس از رفع علت شکست می‌تواند با دلیل ثبت‌شده حداکثر یک بار همین بودجه را تمدید کند، همچنان در سقف روزانهٔ نصب. تکرار درخواست مشتری یا متخصص بودجه را تمدید نکند. ثبت/تأیید رسید و محاسبهٔ اختلاف با ترب فراخوانی AI مستقل ندارند؛ تحلیل هم‌ارزی لازم در همان ارزیابی نهایی یا داوری گروه‌بندی شود.

### تخمین مصرف و مبلغ بسته

تخمین مبنا برای پروندهٔ کامل عمدتاً متنی با ۶ تا ۹ نوبت و حدود ۸ تا ۱۲ فراخوانی، **۲۰٬۰۰۰ ورودی و ۵٬۰۰۰ خروجی قابل‌صورتحساب** است: مشتری ۴٬۰۰۰/۱٬۰۰۰، متخصص ۵٬۰۰۰/۱٬۰۰۰، محل ۵٬۰۰۰/۱٬۵۰۰ و کارهای کمکی ۶٬۰۰۰/۱٬۵۰۰. این اعداد اندازه‌گیری نشده‌اند. سناریوی بالاتر ۴۰٬۰۰۰/۱۰٬۰۰۰ مبنای سقف اولیه است؛ سقف رزرو باید مستقل از این تخمین در کد اعمال شود.

پروندهٔ بی‌پیشنهاد تقریباً سهم مرحلهٔ مشتری و اعتراض قیمتیِ عادی پس از آمادگی مرجع حدود ۳٬۰۰۰/۱٬۰۰۰ مصرف دارد. برای پروندهٔ نیازمند داوری، حدود ۴٬۰۰۰ ورودی و ۱٬۰۰۰ خروجی اضافی به‌عنوان تخمین اولیهٔ اندازه‌گیری‌نشده در نظر گرفته شود؛ سقف و تکرار طبق بودجهٔ عملیاتی بالاست. bootstrap بررسی قیمتی مدل ندارد. تصویر، صوت، OCR، ابزار جست‌وجوی پولی، RAG، زیرساخت و اشتراک خصوصی در تخمین متنی نیستند.

قیمت واقعی بسته در آینده از میانگین هزینهٔ کل API پرونده‌های نماینده و نرخ تبدیل ارز ثبت‌شده محاسبه می‌شود؛ عدد ۲٬۰۰۰ تومان دمو فقط نمونهٔ رابط و آزمایش پرداخت است. جدول تعرفهٔ مدل‌ها در این سند قرار نگیرد. فرمول هزینهٔ متنی، مجموع حاصل‌ضرب ورودی و خروجی قابل‌صورتحساب در تعرفهٔ متناظر، تقسیم بر یک میلیون است؛ ابزار جدا حساب می‌شود. تخفیف cache و Batch در مبنای اولیه فرض نشود.

در حالت live، تعرفهٔ ثبت‌شده با تاریخ و مدل در تنظیمات لازم است؛ برای سقف هزینه از بیشترین نرخ قابل اعمال ارائه‌دهنده استفاده شود. تعرفهٔ اوج و غیر اوج DeepSeek در گزارش هزینه متمایز باشد؛ اگر نرخ دقیق زمان فراخوانی معلوم نیست، هزینه «برآورد محافظه‌کارانه» است. سقف هزینهٔ live کل نصب به‌صورت پیش‌فرض روزانه ۱ دلار است و همهٔ پرونده‌ها، اعتراض‌ها و تلاش مجدد را شامل می‌شود. نبود تنظیم معتبر یا عبور از سقف، فراخوانی را متوقف کند. این سقف مجوز اجرای آزمایش پولی در مرحلهٔ مستندسازی نیست. [تعرفهٔ DeepSeek](https://api-docs.deepseek.com/quick_start/pricing/)، [تعرفهٔ OpenAI](https://developers.openai.com/api/docs/pricing)

### RAG؛ یادداشت آینده

بازیابی سوابق معتبر و مستندات فنی مرتبط می‌تواند برای افزایش دقت و آزمودن مدل ارزان‌تر بررسی شود. **فعلاً RAG، embedding، پایگاه برداری و سرویس وابسته پیاده‌سازی نشوند.** انتخاب ساختاریافتهٔ سوابق با SQL برای مقایسهٔ قیمت جزو MVP است و به زیرساخت RAG نیاز ندارد. RAG معادل آموزش مجدد مدل نیست و کاهش هزینه یا افزایش دقت آن باید با آزمون سنجیده شود.

## ۸. معماری و قرارداد پیاده‌سازی

### فناوری و اجرا

یک مخزن با پوشه‌های `frontend/` و `backend/` بساز. API و worker دو فرایند از یک بستهٔ پایتونی و یک مدل دامنه‌اند؛ تقسیم به میکروسرویس لازم نیست.

| لایه | انتخاب قطعی | مسئولیت |
| --- | --- | --- |
| رابط | Next.js با App Router، React، TypeScript strict و Tailwind CSS؛ Node.js 24 LTS و npm | صفحات، فرم و نمایش؛ عبور درخواست‌های `/api` به بک‌اند |
| بک‌اند | Python 3.13، FastAPI، Uvicorn، Pydantic 2 و pydantic-settings | API، نشست و مجوزها، قواعد محصول، محاسبات و تنظیمات |
| داده | PostgreSQL 18، SQLAlchemy 2 با AsyncSession و درایور asyncpg | ذخیره، تراکنش، قیدها و قفل ردیف؛ DATABASE_URL از نوع `postgresql+asyncpg://...` |
| migration | Alembic با پیکربندی async | تغییر نسخه‌دار schema؛ اجرای صریح قبل از API و worker |
| AI | AiService و قرارداد AiProvider در Python؛ langchain-core و langchain-deepseek با ChatDeepSeek | LangChain برای اتصال async و خروجی Pydantic؛ کنترل مراحل، بودجه و retry در سرویس برنامه |
| کارهای پس‌زمینه | worker پایتونی و جدول Job در PostgreSQL | مهلت‌ها، اجرای AI، داوری، ارزیابی و پیگیری بازپرداخت |
| ابزارها | uv و uv.lock؛ pytest، pytest-asyncio، HTTPX، Ruff و mypy؛ Playwright برای E2E | محیط پایتون تکرارپذیر، کنترل کیفیت بک‌اند و آزمون رابط کامل |

دلیل این انتخاب‌ها: کاربر با Python راحت‌تر است، بنابراین نگهداری قواعد و اتصال AI در این زبان انجام می‌شود. PostgreSQL با نوشتن هم‌زمان مشتری، متخصص و worker و کنترل تراکنشی این محصول تناسب دارد. Next.js روی تجربهٔ وب تمرکز می‌کند. LangChain رابط مشترک مدل و تبدیل خروجی را فراهم می‌کند تا مقایسه یا افزودن ارائه‌دهنده در آینده نیازمند تغییر قواعد محصول نباشد؛ تفاوت پارامترها و مصرف هر ارائه‌دهنده همچنان در آداپتر بررسی شود. این انتخاب‌ها بر پایهٔ تناسب پروژه‌اند و نتیجهٔ بنچمارک سرعت، کاهش قطعی هزینه یا اثبات برتری عمومی یک زبان نیستند.

نسخه‌های پایدار سازگار در `backend/uv.lock` و `frontend/package-lock.json` قفل و نسخهٔ Python در `.python-version` بستهٔ بک‌اند ثبت شوند؛ نسخهٔ دقیق patch در آغاز ساخت تعیین شود. توسعه و آزمون یکپارچگی هر دو PostgreSQL داشته باشند. Redis، Celery و زیرساخت RAG در MVP اضافه نشوند. مبنای فنی: [FastAPI async](https://fastapi.tiangolo.com/async/)، [SQLAlchemy asyncio](https://docs.sqlalchemy.org/en/20/orm/extensions/asyncio.html)، [Alembic async](https://alembic.sqlalchemy.org/en/latest/cookbook.html#using-asyncio-with-alembic)، [مدیریت پروژه با uv](https://docs.astral.sh/uv/guides/projects/).

در `backend/app/` لایه‌های API، schema، domain، repository، provider و worker جدا باشند. API و worker همان سرویس‌های دامنه را فراخوانی کنند. تنها بک‌اند به پایگاه، مدارک خصوصی و کلید AI دسترسی داشته باشد؛ تصمیم مالی، امتیازدهی و مجوز در TypeScript تکرار نشود. بررسی سریع فرم در مرورگر صرفاً برای تجربهٔ کاربر است و سرور همهٔ ورودی‌ها را دوباره اعتبارسنجی می‌کند.

هر درخواست API و هر کار worker یک AsyncSession مستقل داشته باشد؛ session میان taskهای هم‌زمان مشترک نباشد. رزرو سهمیه، انتخاب و تأیید در تراکنش کوتاه با قفل ردیف مربوط و قیود یکتا انجام شوند. هنگام انتظار پاسخ شبکهٔ AI یا آداپتر پرداخت، تراکنش و قفل پایگاه باز نماند؛ رزرو ابتدا commit و نتیجه در تراکنش تازه با کنترل نسخه اعمال شود.

worker هر ۱۰ ثانیه کار موعدرسیده را با `SELECT ... FOR UPDATE SKIP LOCKED` رزرو کند؛ lease پنج‌دقیقه‌ای و شناسهٔ تلاش ذخیره شود. رزرو قبل از اجرای کار commit شود و فقط مالک lease فعلی بتواند نتیجه را اعمال کند. تکرار پس از قطع فرایند، اثر دامنه یا انتقال وجه دوباره نسازد؛ وضعیت مبهم فراخوانی پولی طبق قاعدهٔ رزرو مصرف حفظ شود. FastAPI BackgroundTasks جای این صف پایدار نیست. پیش از نمایش و تغییر حساس پرونده نیز سررسیدها با همان منطق تطبیق داده شوند. ساعت تزریقی در آزمون جای انتظار واقعی را بگیرد. [قفل و SKIP LOCKED در PostgreSQL](https://www.postgresql.org/docs/current/sql-select.html)

`compose.yaml` در ریشه، سرویس `db` با PostgreSQL 18، healthcheck، volume پایدار و پورت متصل به loopback فراهم کند؛ پروفایل `test` یک `db-test` مجزا داشته باشد. API، worker و رابط در توسعه روی میزبان اجرا شوند. استفاده از PostgreSQL نصب‌شده با DATABASE_URL هم پشتیبانی شود؛ نبود Docker باعث تغییر نوع دیتابیس نشود. فایل‌های خصوصی در مسیر تنظیم‌شدهٔ بک‌اند خارج از فایل‌های عمومی وب نگهداری شوند. هیچ volume یا دادهٔ موجود در شروع حذف نشود.

اجرای پیش‌فرض: رابط روی `127.0.0.1:3000` و API روی `127.0.0.1:8000`، با `APP_MODE=demo`. مرورگر درخواست‌های `/api/...` را به همان origin رابط می‌فرستد و rewrite سمت Next.js آن‌ها را بدون اجرای قواعد محصول به FastAPI عبور می‌دهد؛ پاسخ Set-Cookie و ارسال Cookie حفظ شوند. CORS باز یا دسترسی مستقیم مرورگر به پورت API لازم نیست. `NODE_ENV=production` برای آزمون build رابط با حالت demo سازگار بماند؛ حالت محصول واقعی و نشر عمومی خارج از تحویل فعلی است. [Next.js rewrites](https://nextjs.org/docs/app/api-reference/config/next-config-js/rewrites)

### هویت و نگهداری داده

ورود نمایشی از فهرست حساب‌های seed و فقط در حالت demo انجام شود؛ FastAPI شناسهٔ حساب موجود را به نشست تصادفی با انقضای ۸ ساعت تبدیل کند. توکن نشست هش‌شده در پایگاه و کوکی HttpOnly با SameSite=Lax و Path=/، بدون Domain صریح ذخیره شود؛ روی HTTPS ویژگی Secure فعال باشد. تغییر نقش با تغییر نشست انجام شود و کاربر نتواند با فیلد `role` در درخواست مجوز بگیرد. FastAPI درخواست‌های تغییردهنده را فقط با Origin برابر WEB_ORIGIN پیکربندی‌شده بپذیرد؛ مقدار پیش‌فرض `http://127.0.0.1:3000` است. هدرهای عبوری فقط از پراکسی محلی مورد اعتماد پذیرفته شوند؛ Next.js مرجع مستقل نشست یا نقش نسازد.

تعویض حساب و جلو‌بردن ساعت دمو در پنل مجزای «کنترل دمو» باشند. دستکاری ساعت فقط برای پشتیبان و فقط در نصب نمایشی مجاز است؛ ساعت سیستم یا فایل‌های خارج پروژه عوض نشوند. در دادهٔ seed، هیچ تلفن، نشانی یا نام شخص واقعی استفاده نشود.

فایل مدرک و متن چت پس از ۳۰ روز از بستن پرونده پاک شوند؛ رویداد، نسخهٔ توافق و ارزیابی بی‌نام تا ۱۸۰ روز بمانند. سابقهٔ مرجع فقط با رضایت برای استفادهٔ بی‌نام نگهداری شود؛ لغو رضایت آن را از مرجع بعدی خارج کند. نشانی لازم تعمیرگاه نمونه است؛ VIN، پلاک و نشانی منزل جمع‌آوری نشوند. کلید سرویس، نشست و محتوای خصوصی در log یا مخزن قرار نگیرند. پاک‌سازی در worker و reset صریح دمو قابل آزمون باشد.

### موجودیت‌های لازم

تمام موجودیت‌ها UUID بومی PostgreSQL و زمان UTC از نوع TIMESTAMPTZ دارند؛ موارد قابل تغییر، `revision` صحیح افزایشی دارند. مبلغ صحیح تومان در BIGINT با کنترل محدودهٔ عدد صحیح امن JSON/JavaScript ذخیره شود. محاسبات مالی و میانگین در Python با int یا Decimal انجام و گردکردن مبلغ مثبت به تومان با ROUND_HALF_UP باشد؛ از float برای پول استفاده نشود. دادهٔ ساختاریافتهٔ سیاست و خروجی AI می‌تواند JSONB باشد، ولی مالکیت، رابطه و قیود اصلی در ستون و constraint ذخیره شوند. Pydantic مرجع اعتبارسنجی اقلام، API و خروجی‌های مدل است.

| موجودیت | حداقل فیلدها و قیود |
| --- | --- |
| `User`, `Session`, `SpecialistProfile` | نقش، نام نمونه، نشست، خدمات/خودرو/شهر، حالت مراجعه، فعال‌بودن |
| `ServiceTemplate`, `Coverage` | کد عملیات، پرسش‌ها، دادهٔ لازم، زمان مرجع و منشأ نمونه، پوشش و مشخصات قطعه |
| `Request`, `RequestVersion` | مالک، خودرو و خدمت، نوع پیشنهاد مجاز، نشانه‌ها، محله، بازهٔ مراجعه، وضعیت، نسخهٔ دامنه، revision، مهلت، پایان پیشنهادگیری و سیاست بازپرداخت |
| `Offer`, `OfferVersion` | درخواست و نسخهٔ مبنا، متخصص، نوع قیمت، سناریو و اقلام، شروط، زمان، اعتبار، وضعیت؛ یک پیشنهاد فعال برای هر متخصص/درخواست |
| `Selection` | درخواست، متخصص و نسخهٔ پیشنهاد، مهلت پذیرش، تأیید طرفین و وضعیت؛ حداکثر یک انتخاب باز/فعال |
| `AgreementVersion`, `Approval` | انتخاب، نسخهٔ مبنا، اقلام، شروط، علت تغییر، شواهد و وضعیت؛ تأیید یکتا برای هر طرف/نسخه و حداکثر یک توافق فعال برای هر انتخاب |
| `Expense`, `ExpenseVersion`, `ReceiptReview`, `Attachment`, `Completion` | اقلام، پرداخت‌کننده، منشأ، هش فایل، کیفیت، نسخه و تأیید/رد مشتری با دلیل، زمان واقعی و نتیجه؛ رسید تکراری علامت‌گذاری شود |
| `Dispute`, `DisputeStatement`, `DisputeDecision`, `Settlement` | همکاری و طرفین، اقلام و مبالغ ادعا، اظهارات نسخه‌دار، snapshot شواهد، مهلت، وضعیت، حکم و دلایل و اقلام پذیرفته‌شده، منشأ توافق/AI و مبلغ تسویه؛ یک روند برای هر پرونده و یک اعمال برای هر حکم |
| `ReferenceCase`, `ReferenceSnapshot` | گروه مقایسه، مبلغ نهایی و اجزا، رضایت، وضعیت اعتبار، نمونه‌بودن، شناسهٔ سوابق و آمار ثابت |
| `Evaluation`, `Appeal` | ورودی و نسخه‌ها، قضاوت تغییرات، دلیل، شواهد، نسخهٔ مدل/سیاست، نتیجهٔ بازبینی و مهلت |
| `AiRun`, `BudgetReservation` | پرونده، هدف، مرحله، نسخهٔ ورودی، تلاش، وضعیت، توکن واقعی/رزرو، نرخ و هزینهٔ برآوردی؛ جداسازی بودجهٔ مشتری و عملیات |
| `Payment`, `Refund` | مبلغ، شناسهٔ یکتا، وضعیت، مالک، استحقاق، دلیل، نسخهٔ سیاست و انتقال؛ یک استرداد کامل برای هر پرداخت |
| `PartOption` | قطعه، لینک، فروشنده، قیمت، زمان مشاهده، تحویل، منشأ دستی/نمونه و تأیید سازگاری |
| `PartPriceSnapshot`, `PartPriceCheck` | نسخهٔ فاکتور و ردیف، قیمت‌ها و مشخصات فروشندگان مستقل، زمان مشاهده/خرید و مدرک و تأیید منبع، هم‌ارزی، میانه و نسبت اختلاف، نسخهٔ سیاست، وضعیت و رخداد یکتای امتیاز |
| `PolicyVersion`, `Job`, `AuditEvent` | تنظیمات تغییرناپذیر هر نسخه، کار موعدرسیده و تلاش، عامل و دلیل رویداد |

طرح فیزیکی جدول و نام فیلدهای فرعی در اختیار ایجنت است؛ این موجودیت‌ها و قیود نباید در state مرورگر یا حافظهٔ فرایند خلاصه شوند. AI خروجی را فقط برای همان نسخهٔ ورودی تحویل دهد؛ پاسخ دیررس پس از تغییر داده با وضعیت `stale` نگه داشته و روی نسخهٔ تازه اعمال نشود.

### انتقال وضعیت‌ها

| بخش | انتقال‌های مجاز |
| --- | --- |
| درخواست | `draft → open → selecting → assigned → in_progress → completion_review → completed`؛ انتخاب ردشده/منقضی از `selecting` به `open` و تعویض مجاز قبل از کار از `assigned` به `open` برمی‌گردد؛ بسته‌شدن دریافت پیشنهاد با این برگشت بازنشانی نمی‌شود |
| پایان درخواست بدون کار | `open → closed_unselected` فقط پس از پایان دریافت پیشنهاد و نبود پیشنهاد قابل انتخاب، انتخاب در انتظار پذیرش و همکاری پذیرفته‌شده؛ پیش از مهلت، خالی‌بودن فهرست باعث بستن درخواست نشود. نبود پیشنهاد معتبر در پایان مهلت با علت `no_valid_offer` بسته می‌شود؛ انصراف بدون اختلاف به `cancelled` و انصراف دارای اختلاف هزینه به `dispute_open` |
| انتخاب | `pending → accepted/rejected/expired`؛ انتخاب پذیرفته‌شده قبل از کار می‌تواند `ended` شود و طبق سهمیه جایگزین شود |
| توافق | `proposed → active/rejected/expired/void`؛ با فعال‌شدن نسخهٔ تازه، قبلی `superseded` شود؛ با انصراف یا پایان همکاری، فعال به `ended` برود و سابقه حفظ شود. اولین پیش‌نویس پس از ۲۴ ساعت یا پایان اعتبار پیشنهاد، هرکدام زودتر، منقضی شود؛ تغییر توافق فعال ۲۴ ساعت اعتبار مستقل داشته باشد و انقضای پیشنهاد قدیمی مانعش نباشد. شروع پیش از دو تأیید مجاز نیست |
| اجرا | شروع فقط با انتخاب پذیرفته، توافق فعال و نوبت جاری تأییدشده؛ پایان عادی `completion_review → completed` با تأیید مشتری، رسیدهای تأییدشده و صورت‌حساب منطبق؛ اختلاف از همکاری پذیرفته‌شده یا انصراف آن به `dispute_open` و پس از حل، به `closed_settled/closed_adjudicated` |
| رسید | نسخهٔ ارسالی `pending → confirmed/rejected` با اقدام مشتری؛ ویرایش نسخهٔ تازه با `pending`؛ نتیجهٔ داوری در ستون مستقل و بدون تغییر نظر ثبت‌شدهٔ مشتری |
| اختلاف | `dispute_open → reviewing → resolved_by_ai` یا پیش از اعمال حکم `resolved_by_agreement`؛ `reviewing → needs_evidence → reviewing` حداکثر یک بار، و خطای فنی/بودجه به `awaiting_ai → reviewing` طبق سهمیه؛ اعمال حکم یا توافق، پرونده و همکاری را می‌بندد |
| ارزیابی | `queued → running → completed/needs_review/failed`؛ بازبینی نتیجهٔ تازه می‌سازد و سابقه را حفظ می‌کند |
| پرداخت | `created → succeeded/failed`؛ کاربر یا مدل نمی‌تواند مستقیماً `succeeded` را اعلام کند؛ نتیجه از آداپتر پرداخت می‌آید |
| بازپرداخت | طبق بخش ۶؛ تأیید استحقاق و موفقیت انتقال، دو رخداد مستقل |

برای هر تغییر حساس `expectedRevision` لازم است؛ اختلاف نسخه پاسخ 409 و نمایش نسخهٔ تازه بدهد. ایجاد انتخاب، دو تأیید توافق، تأیید رسید، اعمال حکم/توافق حل اختلاف، رزرو مصرف و انتقال پرداخت/بازپرداخت در تراکنش باشند. قیود یکتا در پایگاه اعمال شوند؛ کنترل رابط به‌تنهایی کافی نیست. درخواست با `Idempotency-Key` تکراری و محتوای یکسان همان نتیجه را برگرداند؛ محتوای متفاوت با همان کلید خطا بدهد. داوری و توافق هم‌زمان نباید دو تسویه بسازند؛ تغییر اظهارات یا نسخهٔ مدرک، snapshot قبلی و پاسخ AI آن را منقضی کند.

### قرارداد عملیات

endpointهای FastAPI زیر `/api` با ورودی و خروجی Pydantic تعریف شوند. حداقل عملیات: ورود/خروج demo، ایجاد و تکمیل درخواست، پرداخت نمونه، انتشار، فهرست مرتبط، ثبت/ویرایش/پس‌گرفتن پیشنهاد، انتخاب و پذیرش آن، نسخه و تأیید توافق و نوبت، لغو/تعویض، ثبت قطعه و مخارج، تأیید/رد رسید، ثبت/تأیید منبع قیمت و مشاهدهٔ هشدار، درخواست/تأیید پایان، ثبت اختلاف و اظهارات و پیشنهاد حل آن، مشاهدهٔ حکم و تسویه، پیام مرحلهٔ AI، ارزیابی و اعتراض، درخواست/پیگیری بازپرداخت و بررسی پشتیبان.

OpenAPI از همین مدل‌ها تولید و در `/api/openapi.json` ارائه شود؛ فایل قراردادی `contracts/openapi.json` نیز بدون نیاز به دیتابیس یا کلید مدل از کد بک‌اند صادر شود. typeهای TypeScript با openapi-typescript از این فایل تولید شوند و دستی ویرایش نشوند. نام‌گذاری JSON به camelCase و کد Python به snake_case با aliasهای Pydantic باشد. آزمون قرارداد باید ناهماهنگی schema صادرشده و typeهای تولیدشده را تشخیص دهد. فرانت با fetch و typeهای تولیدشده API را صدا بزند؛ schema مستقل رقیب در فرانت ساخته نشود. [Pydantic](https://docs.pydantic.dev/latest/)، [openapi-typescript](https://openapi-ts.dev/introduction)

هر عملیات باید نقش مجاز، وضعیت مبدأ، نسخهٔ مورد انتظار و نتیجهٔ خطا را مشخص کند. قرارداد مشترک پاسخ خطا `{code, message, fieldErrors?, currentRevision?}` باشد؛ کدهای پایه `VALIDATION_ERROR`، `FORBIDDEN`، `VERSION_CONFLICT`، `INVALID_STATE`، `QUOTA_EXCEEDED` و `SERVICE_UNAVAILABLE` باشند. handler خطای اعتبارسنجی FastAPI نیز خطای 422 را به همین قالب تبدیل کند و جزئیات داخلی یا ورودی محرمانه را افشا نکند. دریافت خروجی مدل از صف با شناسهٔ `AiRun` و polling انجام شود؛ انتظار طولانی صفحه را قفل نکند.

قراردادهای AI حداقل این فیلدها را داشته باشند: پرسش‌ها با `id/type/options/required`، خلاصه با `facts/unknowns/suggestedOfferType`، اقلام مخارج با `sourceText/amount/payer/needsConfirmation`، ارزیابی با `changeId/verdict/reason/evidenceIds/missingFields`، و بررسی قیمت با `offerVersionId/comparable/verdict/referenceSnapshotId/reason`. شناسهٔ شواهد باید واقعاً در ورودی همان بررسی وجود داشته باشد؛ متن دلیل بدون شاهد، کافی نیست.

قرارداد داوری شامل `disputeId/inputSnapshotId/status/lineDecisions/reason/evidenceIds/missingFields` باشد؛ هر تصمیم قلم `claimItemId/acceptedQuantity/acceptedAmount/verdict/reason/evidenceIds` دارد. مقدار و مبلغ نامنفی و حداکثر برابر ادعای همان قلم باشند؛ هیچ قلم جدید یا تکراری پذیرفته نشود. snapshot اقلام مورد اختلاف و بدون اختلاف را جدا نگه دارد؛ جمع نهایی و سهم متخصص را کد با حفظ پرداخت‌کنندهٔ هر قلم محاسبه کند. وضعیت `needs_evidence` هیچ تسویه‌ای اعمال نمی‌کند؛ فقط `decided` با همهٔ اقلام مورد اختلافِ تعیین‌تکلیف‌شده و نسخهٔ جاری قابل اعمال است. ورودی ارزیابی قیمت قطعه شامل نسخهٔ رسید، وضعیت تأیید مشتری، snapshot قیمت‌های ترب و محاسبهٔ عددی برنامه باشد؛ AI دربارهٔ هم‌ارزی توضیح می‌دهد و آستانه یا قیمت مرجع را نمی‌سازد.

`MockPaymentGateway` موفقیت، شکست و پاسخ تکراری پرداخت/بازپرداخت را شبیه‌سازی کند. شناسهٔ تراکنش و رسید نمونه سمت سرور تولید شوند. مسیر واقعی بانکی، دریافت شماره کارت و اتصال درگاه ساخته نشوند.

## ۹. دادهٔ نمونه و سناریوهای آزمون

Seed تکرارپذیر شامل ۲ مشتری، ۱ پشتیبان، ۶ متخصص قابل ورود و ۳ قالب خدمت باشد. ۳ متخصص به درخواست اصلی مرتبط و ۳ متخصص عمداً به‌علت شهر، خودرو یا تخصص نامرتبط باشند. برای مرجع هر قالب ۱۲ پروندهٔ فرضی از ۴ متخصص آرشیوی مستقل از پیشنهاددهندگان سناریوی جاری ساخته شود؛ هرکدام ۳ پرونده تا قید سهم ۴۰٪ رعایت شود. همهٔ نام‌ها و قیمت‌ها صریحاً نمونه باشند. این داده برای اجرای سیاست و آزمون است و نتیجهٔ تحقیق بازار نیست.

سناریوهای قابل انتخاب از کنترل دمو:

1. **مسیر کامل:** درخواست کلاچ، سه پیشنهاد هم‌دامنه، انتخاب، پذیرش، توافق، ثبت مخارج و پایان.
2. **افزایش موجه:** یافتهٔ تازه و مدرک نمونه، تغییر با دو تأیید و ارزیابی موجه.
3. **افزایش ناموجه:** همان دامنه با افزایش بدون شاهد؛ مشتری می‌پذیرد ولی ارزیابی ناموجه است. کاهش قیمت نیز نمونهٔ جدا بدون اثر منفی داشته باشد.
4. **قطعهٔ مشتری:** هزینه در مبلغ کل مقایسه هست ولی دوباره به مبلغ قابل پرداخت به متخصص اضافه نمی‌شود.
5. **bootstrap:** با وجود پیشنهاد منصفانه، درخواست بازپرداخت پذیرفته شود و فراخوانی قیمتی AI صفر باشد.
6. **بدون پیشنهاد:** با ساعت آزمون، پایان ۲۴ ساعت و یک بازپرداخت خودکار؛ پیش از مهلت بازپرداخت این مسیر فعال نشود.
7. **مرجع آمادهٔ نمایشی:** یک پیشنهاد منصفانه کنار گران‌ها باعث رد قیمتی شود؛ در حالت همهٔ پیشنهادها نامنصفانه، تأیید شود.
8. **کمبود شواهد/قطع AI:** توافق دستی ممکن باشد؛ بررسی نامعلوم تا ۴۸ ساعت به مسیر تعیین‌شدهٔ پشتیبانی و بازپرداخت برسد.
9. **رقابت و دسترسی:** انتخاب هم‌زمان دو متخصص، تأیید نسخهٔ قدیمی، تعویض و تلاش متخصص قبلی برای چت یا دیدن مدارک تازه.
10. **تکرار مالی و مصرف:** پرداخت/بازپرداخت تکراری، timeout، رزرو هم‌زمان و پایان سهمیه؛ هیچ برداشت، استرداد یا اعتبار مضاعف ایجاد نشود.
11. **افزایش در اولین توافق:** پیشنهاد منتخب یک مبلغ دارد و اولین توافق با همان دامنه مبلغ بالاتری؛ افزایش بی‌دلیل در ارزیابی ثبت شود، حتی اگر مشتری آن را پذیرفته باشد. لغو بعدی رخداد احرازشده را پاک نکند.
12. **رسید و قیمت ترب:** تأیید مشتری روی یک نسخه، ویرایش و ازاعتبارافتادن تأیید قبلی؛ فاکتور بیش از آستانه با سه قیمت هم‌مبنای نمونه، هشدار و ثبت یک علت منفی. قیمت ارزان‌تر، برند متفاوت، منبع تأییدنشده، قیمت پس از خرید و قطعهٔ خریداری‌شده توسط مشتری اثر منفی خودکار نسازند.
13. **داوری لازم‌الاجرا:** رد رسید/صورت‌حساب، پایان فرصت توافق، حکم دربارهٔ اقلام و اعمال بدون تأیید مجدد مشتری یا متخصص. مبلغ و منشأ تسویه دیده شوند، نظر مشتری دربارهٔ رسید عوض نشود و پرداخت واقعی ایجاد نشود. توافق هم‌زمان با حکم، ورودی منقضی، حکم ناقص، تکمیل شواهد و قطع مدل نیز پوشش داده شوند.
14. **نوبت گذشته:** پیشنهاد از نظر قیمت معتبر ولی زمان مراجعه گذشته؛ انتخاب و پذیرش رد شوند. نوبت همکاری فعال با دو تأیید جابه‌جا شود و مهلت پیشنهادگیری یا سهمیه بازنشانی نشود.

قالب‌های ارزیابی mock از خروجی live مستقل باشند. برای سنجش مدل واقعی، فایل ۲۴ پروندهٔ فارسی با برچسب مورد انتظار بساز: ۴ تکمیل درخواست، ۴ استخراج مخارج، ۴ ارزیابی افزایش، ۴ انصاف پیشنهاد، ۴ ورودی ناقص/متناقض و ۴ داوری اختلاف. داوری شامل هزینهٔ مستند، ادعای بی‌مدرک، رسید ردشده و نیاز به تکمیل شواهد باشد؛ مبلغ و اقلام مجاز هر حکم در مرجع آزمون معلوم باشند. برچسب‌ها باید جدا از پاسخ مدل نوشته شوند و در گزارش، مصنوعی‌بودن مجموعه معلوم باشد. این مجموعه جای ارزیابی انسانی برای عرضه را نمی‌گیرد.

## ۱۰. معیار پذیرش، سنجه‌ها و ترتیب ساخت

### معیار پایان MVP

- همهٔ سناریوهای بخش ۹ با دادهٔ پایدار و نقش‌های مستقل اجرا شوند؛ تازه‌سازی مرورگر یا راه‌اندازی دوبارهٔ سرور سابقه را از بین نبرد.
- قیمت آزاد، دو تأیید همان نسخه در توافق عادی، استقلال توافق داوطلبانه از AI، اعمال حکم معتبر بدون تأیید دوباره، تشخیص انواع پیشنهاد و جداسازی مبلغ کل از سهم متخصص با آزمون قواعد پوشش داده شوند.
- مرزهای دقیق ۲۴ ساعت، ۲ ساعت، ۴۸ ساعت، زمان مراجعه، اعتبار پیشنهاد، مهلت اظهارات/تکمیل شواهد و نسخهٔ سیاست بازپرداخت با ساعت تزریقی بررسی شوند. درخواست بدون پیشنهاد پیش از مهلت باز بماند و پایان مهلت، انتخابِ هنوز معتبر و در انتظار پذیرش را لغو نکند؛ بازپرداخت ردشده هیچ کار انتقالی نسازد.
- کنترل مالکیت، منع چت سایر پیشنهاددهندگان و لغو دسترسی منتخب قبلی هم در API و هم در رابط آزموده شوند.
- هم‌زمانی انتخاب/تأیید، تأیید رسید و ویرایش آن، حکم و توافق حل اختلاف، رزرو بودجه و پرداخت/بازپرداخت تکراری آزمون یکپارچگی پایگاه داشته باشند. حکم دیررس یا تکراری تسویهٔ تازه نسازد.
- معیار مرجع، حذف دادهٔ خوداظهاری/تکراری، عدم آلودگی دادهٔ واقعی با نمونه یا حکمِ تأییدنشدهٔ مستقل، انصاف یک پیشنهاد و نبود امتیاز منفی برای کاهش قیمت آزموده شوند. آستانهٔ فاکتور، هم‌ارزی و زمان قیمت ترب، ثبت علت منفی و منع دوباره‌شماری آن پوشش داده شوند. ترتیب مقایسه با قیمت مساوی، امتیاز معلوم/نامعلوم و زمان متفاوت پایدار و مستقل از ترتیب ورود پیشنهادها باشد.
- بک‌اند با pytest، pytest-asyncio و HTTPX برای API آزموده شود؛ آزمون یکپارچگی و هم‌زمانی به PostgreSQL جداگانهٔ تست متصل باشد. TEST_DATABASE_URL باید به پایگاه تست متفاوت از دادهٔ دمو اشاره کند و پاک‌سازی فقط به schema همان اجرای تست محدود باشد. فراخوانی خارجی مدل در آزمون با mock کنترل شود.
- مسیر واقعی `DeepSeekProvider` و LangChain با پاسخ شبکهٔ شبیه‌سازی‌شده آزموده شود، نه فقط با جایگزینی MockAiProvider: هر تلاش یک درخواست مدل، پارامتر صریح thinking و سقف خروجی، نبود retry داخلی در timeout/429، JSON ناقص همراه با ثبت مصرف، و usage شامل استدلال بدون دوباره‌شماری پوشش داده شوند. اصلاح قالب و تکرار مجاز فقط از AiService و داخل سقف تلاش/بودجه انجام شوند؛ این آزمون‌ها تماس پولی ندارند.
- Ruff و mypy بک‌اند، `typecheck` و `lint` و `build` فرانت، کنترل انطباق OpenAPI و typeهای تولیدشده و E2E با Playwright موفق باشند. تست‌ها پس از آماده‌سازی وابستگی‌ها فقط به سرویس‌های محلی نیاز داشته باشند؛ AI پولی یا اتصال زندهٔ ترب پیش‌نیاز نیست. خطای live و نداشتن کلید محلی آزموده شود. E2E نیز داده و تنظیمات مجزای تست داشته باشد.
- در صورت اجرای اختیاری live، اعتبار schema در هر ۲۴ پرونده و نبود نقض اختیار مدل/قیمت‌سازی در تمام موارد لازم است؛ حداقل ۲۰ مورد از ۲۴ با نتیجهٔ مرجع طراحی تطابق داشته باشند و هر ۴ مورد داوری، نتیجه و اقلام/مبلغ مجاز مرجع را رعایت کنند. شکست این معیار به اصلاح و گزارش می‌انجامد؛ demo mock همچنان قابل تحویل است و کیفیت live تأییدشده معرفی نشود.
- در گزارش تحویل، حالت‌های واقعاً آزموده‌شده، مدل و تنظیمات، هزینهٔ اندازه‌گیری‌شده یا نبود اندازه‌گیری و محدودیت داده صریح باشند؛ قبولی mock معادل آزمون AI واقعی نیست.

### سنجه‌های داخل محصول

رویدادهای نمایش هزینه، پرداخت، انتشار، ثبت اولین پیشنهاد معتبر، انتخاب، تغییر، تأیید/رد رسید، هشدار فاکتور، ثبت اختلاف، اعمال حکم، پایان، مصرف و نتیجهٔ بازپرداخت ثبت شوند. داشبورد پشتیبان تعداد و میانهٔ زمان اولین پیشنهاد معتبر، میانگین تعداد پیشنهادهای قابل مقایسه، نسبت درخواست‌های بی‌پیشنهاد به درخواست‌های مهلت‌تمام‌شده، و نرخ پرداخت پس از نمایش هزینه را نشان دهد.

شفافیت با نسبت پرونده‌های دارای علت منفی `U/N` و تفکیک علت‌ها، رضایت با میانگین و تعداد پاسخ، مصرف با میانگین و p95 پرونده‌های بسته و سهم پایان سهمیه، داوری با تعداد و زمان تا حکم و بازپرداخت با تعداد، علت و زمان تا انتقال نمایش داده شود. مخرج و تعداد نمونه کنار هر نسبت باشد؛ در مخرج صفر «داده‌ای نیست» نمایش داده شود. دادهٔ نمونه از live جدا باشد. هدف ساخت دمو، قابل‌اندازه‌گیری‌بودن است؛ برای تقاضای واقعی یا رضایت بازار هدف عددی ساختگی تعیین نشود.

### مراحل اجرا

1. **زیرساخت محلی و قواعد:** پوشه‌های frontend/backend، PostgreSQL، تنظیمات پایتون، migration با Alembic، seed، نشست و سرویس دامنه؛ قرارداد Pydantic/OpenAPI و typeهای فرانت؛ آزمون محاسبه، نسخه و زمان.
2. **مسیر کامل با mock:** درخواست و پرداخت نمونه، پیشنهاد، انتخاب و توافق و نوبت، مخارج، تأیید رسید و پایان با همهٔ نقش‌ها.
3. **داوری، ارزیابی و بازپرداخت:** اختلاف و تسویهٔ لازم‌الاجرا، مرجع، سیاست نسخه‌دار، امتیاز، صف پشتیبان، worker و تکرار ایمن اعمال حکم و انتقال.
4. **AI و قطعه:** AiService و آداپتر mock/live با LangChain، سقف و ثبت مصرف، خروجی Pydantic و آزمون ارتباط شبیه‌سازی‌شده، لینک و اطلاعات دستی ترب، snapshot قیمت و هشدار فاکتور؛ بررسی خطا و دسترسی.
5. **تکمیل تحویل:** E2E، نمایش موبایل و دسکتاپ، build، سنجه‌ها، README و سناریوی دموی پنج‌دقیقه‌ای.

برای هر مرحله ابتدا مسیر موفق و شکست‌های مؤثر را کامل کن؛ رابط خالی یا دکمهٔ بی‌عمل را قابلیت تمام‌شده حساب نکن. پیش از مرحلهٔ بعد آزمون مرتبط را اجرا کن. تا پایان موارد لازم ادامه بده و هر مسئلهٔ اجرایی جزئی را با همان قواعد حل و مستند کن.

## ۱۱. قرارداد تحویل به کاربر

ایجنت باید کد frontend/backend، migrationهای Alembic، seed پایتونی، تست‌ها، lockfileهای دو محیط، `compose.yaml` برای پایگاه، `.env.example` بدون کلید، قرارداد OpenAPI و typeهای تولیدشده، README فارسی و `demo-script.md` تحویل دهد. نصب ابزار، ایجاد پروژه و اجرای سرویس مربوط به مرحلهٔ پیاده‌سازی است؛ در مرحلهٔ ویرایش این پلن انجام نشده است.

| محل اجرا | دستور تحویلی | کاربرد |
| --- | --- | --- |
| ریشه | `docker compose up -d db` | PostgreSQL محلی؛ برای نصب موجود فقط DATABASE_URL تنظیم شود |
| `backend/` | `uv sync --locked` | نصب وابستگی‌های قفل‌شدهٔ پایتون |
| `backend/` | `uv run alembic upgrade head` | migration صریح پیش از API و worker |
| `backend/` | `uv run python -m app.seed` | seed تکرارپذیر بدون پاک‌کردن دادهٔ موجود |
| `backend/` | `uv run uvicorn app.main:app --reload --host 127.0.0.1 --port 8000` | API محلی |
| `backend/` | `uv run python -m app.worker` | worker پایدار پایتونی |
| `backend/` | `uv run python -m app.export_openapi` | صدور `contracts/openapi.json` در ریشه بدون اتصال DB/AI |
| `frontend/` | `npm ci` و `npm run api:types` | نصب وابستگی و تولید typeها از قرارداد |
| `frontend/` | `npm run dev -- --hostname 127.0.0.1 --port 3000` | رابط محلی |
| ریشه | `docker compose --profile test up -d db-test` | پایگاه مجزای تست |
| `backend/` | `uv run pytest`، `uv run ruff check .` و `uv run mypy app` | آزمون و کنترل کیفیت بک‌اند |
| `frontend/` | `npm run typecheck`، `npm run lint`، `npm run api:check`، `npm run test:e2e` و `npm run build` | کنترل رابط، انطباق قرارداد، E2E و build |
| ریشه | `uv run --project backend python scripts/dev.py --with-db` | راه‌اندازی هماهنگ پایگاه محلی، API، worker و رابط پس از نصب و آماده‌سازی داده |

اسکریپت `scripts/dev.py` در Windows و Linux مسیرهای کاری و توقف فرایندهای فرزند را مدیریت کند، سلامت اتصال را بررسی و خطای هر سرویس را واضح نشان دهد. با حذف `--with-db` از پایگاه ازپیش‌آماده استفاده شود. اسکریپت وابستگی نصب، migration یا حذف دادهٔ خودکار انجام ندهد؛ README ترتیب اولین راه‌اندازی را مشخص کند. reset فقط برای دادهٔ demo با دستور صریح و پایگاه کنترل‌شده مجاز باشد؛ در شروع دادهٔ موجود پاک نشود.

بک‌اند و worker تنظیمات را با pydantic-settings از محیط یا `.env` ریشه بخوانند. DATABASE_URL، TEST_DATABASE_URL، WEB_ORIGIN، مسیر مدارک، APP_MODE، AI_MODE و تنظیمات مدل/سقف مشخص باشند؛ کلید AI و اطلاعات پایگاه به متغیر عمومی Next.js منتقل نشوند. فرانت فقط تنظیم عبور API سمت سرور و اطلاعات عمومی لازم را دریافت کند.

README باید پیش‌نیازهای Python/uv، Node/npm و PostgreSQL یا Docker، راه‌اندازی بدون کلید، ورود هر نقش، تفاوت AI نمایشی و واقعی، تنظیم سقف‌ها، اجرای سناریوهای زمانی، تأیید رسید، داوری و محدودیت پرداخت و ترب را توضیح دهد. سیاست‌های آستانهٔ انصاف و اختلاف فاحش فاکتور، حداقل سابقه، مهلت و سقف در یک محل در بک‌اند تعریف و هنگام استفاده نسخه‌برداری شوند. `api:check` خروجی تازهٔ OpenAPI و typeها را با قرارداد تحویلی مقایسه کند و در ناسازگاری خطا بدهد. نتیجهٔ آزمون‌ها در گزارش تحویل ثبت شود؛ کار انجام‌نشده به‌عنوان انجام‌شده علامت نخورد.

سناریوی ویدیوی پیشنهادی: ۲۵ ثانیه مسئله، ۴۰ ثانیه درخواست، ۵۰ ثانیه مقایسه، ۶۵ ثانیه توافق و رسید و هشدار قیمت، ۵۵ ثانیه داوری و امتیاز، ۳۰ ثانیه بازپرداخت و ۳۵ ثانیه تصمیم‌های فنی و محدودیت‌ها؛ مجموع ۵ دقیقه. راهنمای ضبط و دادهٔ تکرارپذیر جزو تحویل است؛ ضبط یا ارسال واقعی ویدیو فقط در صورت درخواست کاربر انجام شود.

برای شروع کدنویسی هیچ تصمیم مسدودکنندهٔ بازی باقی نمانده است. کلید مدل و دادهٔ واقعی با مسیرهای صریح mock، bootstrap و ثبت دستی پوشش داده شده‌اند. عرضهٔ عمومی مرحلهٔ جداست و به هویت و پرداخت واقعی، بررسی منابع ترب، دادهٔ معتبر تعمیر، کالیبراسیون سیاست و ارزیابی انسانی مدل نیاز دارد؛ این موارد شرط پایان MVP نمایشی نیستند.
